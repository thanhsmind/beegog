{
  "id": "cph-2",
  "feature": "codex-plugin-first-hybrid",
  "lane": "high-risk",
  "behavior_change": true,
  "title": "install.sh runtime passthrough + E2E: codex plugin-first proves hooks-or-refusal, never skills-only (D2/D3)",
  "action": "In scripts/install.sh: plugin-first branch passes --runtime \"$RUNTIME\" through to onboard_bee.mjs on the APPLY invocation (~:405), not only the plan/preview (~:360) — verify both call sites (advisor upgrade-path note); a refused/blocked onboarding apply keeps aborting via handle_transition_failure -> rollback_plugin (:406, existing D4-REVISED guarantee c) with the fail-closed message extended to name repo-copy or hybrid retry; the DIST_HELPER cleanup invocation (:411) carries the runtime scoping cph-1 added so it cannot strip the codex hooks it just gained (advisor R1). E2E scenarios in scripts/test_installers_e2e.mjs after test 12 (~:502), using sandbox()/stagedSource()/run() helpers: (a) --runtime codex --distribution plugin-first full install -> assert AFTER the cleanup pass (post-full-install end state, advisor R1): .codex/hooks.json present with required matchers + vendored .bee/bin/hooks present + skills resolved from plugin; also assert bee doctor --runtime codex --json reports hooks_file_present ok while trust rows stay blocking (matrix F1 expected); (b) hook-write-impossible fixture (.codex path pre-occupied by a regular file) -> typed refusal surfaced by installer, plugin rolled back, NO skills-only end state; (c) claude plugin-first regression guard: exclusivity unchanged (claude repo-local hook entries still stripped), no codex files appear for --runtime claude.",
  "must_haves": {
    "truths": [
      "E2E: codex plugin-first ends (post-cleanup) with repo-local hooks present and skills from the plugin, asserted on-disk and via doctor hooks_file_present ok",
      "E2E: when the hook write is impossible the installer refuses typed, the plugin is rolled back, and the repo is not left skills-only",
      "E2E: claude plugin-first behavior unchanged — claude hook entries stripped, no codex files for --runtime claude",
      "--runtime is threaded into the apply invocation (install.sh ~:405), not only the preview"
    ]
  },
  "verify": "node scripts/test_installers_e2e.mjs --installer bash && node skills/bee-hive/scripts/test_onboard_bee.mjs",
  "deps": [
    "cph-1"
  ],
  "files": [
    "scripts/install.sh",
    "scripts/test_installers_e2e.mjs"
  ],
  "status": "capped",
  "decisions": [],
  "read_first": [],
  "trace": {
    "worker": "cph2-worker",
    "outcome": "install.sh/.ps1 --runtime passthrough to every onboarding call site; typed-blocked apply aborts with fix-naming messages; 3 E2E scenarios prove hooks-or-refusal end states (codex hybrid green incl. doctor rows, .codex-as-file typed rollback, claude regression guard); independent verify green on main 8196e03",
    "files_changed": [
      "scripts/install.sh",
      "scripts/install.ps1",
      "scripts/test_installers_e2e.mjs"
    ],
    "deviations": [],
    "friction": null,
    "capped_at": "2026-07-18T16:37:17.050Z",
    "behavior_change": true,
    "verification_evidence": "# cph-2 — done report (installer passthrough + E2E hooks-or-refusal)\n\nWorker: cph2-worker (sonnet, isolated harness worktree, rebased onto main\n720448f). Worker commit `991a03d`, merged into main as `8196e03`. 2026-07-18.\n\n## What shipped\n- `install.sh` + `install.ps1`: `--runtime \"$RUNTIME\"` threaded into\n  ONBOARD_FLAGS unconditionally (all onboard_bee.mjs call sites — preview,\n  apply, recheck); repo-copy branch verified a no-op today (runtime only gates\n  `codexHybrid = pluginSource && runtimeCoversCodex`) and threaded for\n  symmetry; stale \"not threaded yet\" comments corrected. Typed-blocked apply\n  confirmed to abort via `handle_transition_failure → rollback_plugin`;\n  failure messages on BOTH the onboarding-apply and DIST_HELPER-preflight\n  paths now name the fixes (repo-copy, or clear the obstacle and retry\n  hybrid). install.ps1 parity, with an in-code note that it has no plugin\n  rollback machinery (pre-existing asymmetry, out of scope).\n- E2E (`test_installers_e2e.mjs`, 3 new scenarios, suite now 19):\n  (a) codex plugin-first → `.codex/hooks.json` carries all 8 lifecycle events\n  incl. the spawn_agent PreToolUse matcher, vendored `.bee/bin/hooks/` has\n  all 10 canonical handlers, no repo-local skill copies, and doctor reports\n  `hooks_file_present: ok` with trust rows `unknown + blocking` (the expected\n  codex-cli 0.144.4 shape).\n  (b) `.codex` pre-created as a file → typed blocked refusal, plugin rolled\n  back, never skills-only. Finding: the refusal fires from\n  plugin_distribution's cleanup probe (raw ENOTDIR string) BEFORE\n  onboarding's polished codexHookWriteBlocker message — still typed, still\n  rolled back; curated-message follow-up filed to backlog.\n  (c) claude plugin-first regression guard: stale claude hook entries still\n  stripped; no codex files appear.\n\n## Verification (fresh command output)\nIndependent orchestrator re-run on main after merge 8196e03: see\n`.bee/workers/cph2-verify.log` — `test_installers_e2e (bash): 19 passed, 0\nfailed`; onboard_bee PASS failures 0; plugin_distribution 38/0; lib_mirror\n4/4; manifest 354 match. (Tail recorded in the cell verify entry.)\n\n## Deviations (accepted)\n- Worker rebased its worktree onto main first (harness worktree was cut at\n  the pre-cph-1 merge-base; clean fast-forward).\n- Fix-options guidance extended to the DIST_HELPER preflight failure path\n  (the path scenario (b) actually exercises).\n- install.ps1 message parity without inventing rollback machinery it never had.\n",
    "verify_output": "Independent orchestrator re-run on main after merge 8196e03: test_installers_e2e bash 19 passed 0 failed; test_onboard_bee PASS failures 0 skipped 1; plugin_distribution 38/0; lib_mirror 4/4; manifest 354 match. Worker commit 991a03d.",
    "verify_passed": true,
    "claimed_at": "2026-07-18T16:20:03.353Z",
    "verify_command": "node scripts/test_installers_e2e.mjs --installer bash && node skills/bee-hive/scripts/test_onboard_bee.mjs",
    "verified_at": "2026-07-18T16:37:16.997Z"
  }
}
