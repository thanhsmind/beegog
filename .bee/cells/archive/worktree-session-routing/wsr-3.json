{
  "id": "wsr-3",
  "feature": "worktree-session-routing",
  "lane": "high-risk",
  "behavior_change": true,
  "title": "D9 routing prose: worktree paved road in bee-hive Session Scout + AGENTS.md rule 14 + rendered trees",
  "action": "Prose only, no new hook (D9). skills/bee-hive/SKILL.md Session Scout: when starting NEW feature work in a checkout that already has another live session's active work — observable as a live cross-session heartbeat plus a non-idle phase in the shared store (decision D9a), or active holds/live-owner lanes — the paved road is bee worktree new --feature <slug> and opening the next session in the printed path; docs-lane work, tiny fixes, and release machinery stay in the MAIN checkout (release always runs in main). AGENTS.block.md template rule 14 rider: same paved road + merge-back sentence (merge from main via bee worktree merge --id <id>; the post-merge configured verify is the semantic-conflict gate). Apply the same rider to the repo's rendered AGENTS.md. Re-render plugin skill trees (scripts/render_plugin_skill_trees.mjs), refresh managed .claude/ + .agents/ copies, run scripts/release_manifest.mjs --write. Keep AGENTS.md within its budget check.",
  "must_haves": {
    "truths": [
      "bee-hive Session Scout and AGENTS.md rule 14 both name the paved road (worktree new for new feature work in an occupied checkout; docs/tiny/release stay in main; merge from main with verify as the semantic gate)",
      "the rider lands in the AGENTS.block.md template so onboarded hosts receive it, not just this repo's AGENTS.md",
      "rendered plugin trees, managed copies, and the release manifest are drift-green after the render",
      "AGENTS.md stays within its budget check"
    ]
  },
  "verify": "node scripts/test_skill_render.mjs && node skills/bee-hive/scripts/test_plugin_distribution.mjs && node scripts/release_manifest.mjs --check && node skills/bee-hive/scripts/test_onboard_bee.mjs",
  "depends_on": [
    "wsr-2"
  ],
  "files": [
    "skills/bee-hive/SKILL.md",
    "skills/bee-hive/templates/AGENTS.block.md",
    "AGENTS.md",
    ".claude/skills/*",
    ".agents/skills/*",
    ".claude-plugin/*",
    "release-manifest.json"
  ],
  "status": "capped",
  "deps": [],
  "decisions": [],
  "read_first": [],
  "trace": {
    "worker": "wsr3-worker",
    "outcome": "D9 routing prose live in bee-hive Session Scout + AGENTS rule 14 (template + rendered), plugin trees re-rendered with the new worktree verbs, manifest regenerated; full configured verify green on integration branch 07f9d9b",
    "files_changed": [
      "skills/bee-hive/SKILL.md",
      "skills/bee-hive/templates/AGENTS.block.md",
      "AGENTS.md",
      "scripts/test_worktree_cli.mjs"
    ],
    "deviations": [],
    "friction": null,
    "capped_at": "2026-07-18T14:14:32.550Z",
    "behavior_change": true,
    "verification_evidence": "# wsr-3 — done report (D9 routing prose + renders)\n\nWorker: wsr3-worker (sonnet, isolated harness worktree, base e0759a6). Worker\ncommit `8cd44b3`, merged into integration branch `wt/worktree-session-routing`\nas `07f9d9b`. 2026-07-18.\n\n## What shipped\n- `skills/bee-hive/SKILL.md`: Session Scout gained the \"Worktree routing (D9)\"\n  paragraph — occupied checkout (live heartbeat + non-idle phase / active\n  holds) → `bee worktree new`; docs/tiny/release stay in main; merge-back via\n  `bee worktree merge` with the post-merge verify as the semantic-conflict gate.\n- `skills/bee-hive/templates/AGENTS.block.md` rule 14: one-sentence rider with\n  the same paved road + merge-back; rendered root AGENTS.md mirrored\n  byte-identically within the BEE block (in the integration tree).\n- Plugin trees `.claude-plugin/skills` + `.codex-plugin/skills` re-rendered —\n  also picking up wsr-1/wsr-2's new verbs; managed `.claude/` + `.agents/`\n  bee-hive copies refreshed via the real `renderSkillBytes` renderer (scoped;\n  the worker deliberately avoided `onboard_bee.mjs --apply` to not touch this\n  machine's live shared `~/.claude/skills` root mid-session).\n- `release_manifest.mjs --write`: manifest regenerated (354 files).\n- Cleanup rider: unused `grantsFileA` dropped from scripts/test_worktree_cli.mjs.\n\n## Verification (fresh command output)\nWorker-side (its worktree): skill_render 27/0, plugin_distribution 36/0,\nmanifest --check 354 match, onboard_bee PASS (0 failures, 1 pre-existing skip),\nworktree_cli 90/90, lib_mirror 4/4.\n\nOrchestrator-side: FULL configured verify (all 25 suites) run on the\nintegration branch after merge 07f9d9b → exit 0 (`FULL_VERIFY_GREEN`), log at\n`.bee/workers/wsr-full-verify.log` in the integration worktree.\n\n## Diff (merge 07f9d9b)\n21 files changed, 1614 insertions(+), 33 deletions(-).\n\n## Deviations (accepted)\n- Managed-copy refresh used the renderer directly instead of a full\n  `onboard_bee.mjs --apply` (which would have best-effort rewritten the live\n  global `~/.claude/skills` shared by concurrent sessions) — same bytes,\n  narrower blast radius.\n",
    "verify_output": "Worker: skill_render 27/0, plugin_distribution 36/0, manifest --check 354 files match, onboard_bee PASS. Orchestrator: FULL configured verify (25 suites) on integration branch after merge 07f9d9b -> exit 0 FULL_VERIFY_GREEN (log .bee/workers/wsr-full-verify.log in integration worktree). Worker commit 8cd44b3.",
    "verify_passed": true,
    "claimed_at": "2026-07-18T14:05:23.195Z",
    "verify_command": "node scripts/test_skill_render.mjs && node skills/bee-hive/scripts/test_plugin_distribution.mjs && node scripts/release_manifest.mjs --check && node skills/bee-hive/scripts/test_onboard_bee.mjs",
    "verified_at": "2026-07-18T14:14:32.501Z"
  }
}
