{
  "id": "g22-3",
  "feature": "gh22-completion",
  "lane": "high-risk",
  "behavior_change": true,
  "title": "doctor three-state (ready/degraded/blocked) + attest verb + version-scoped verdicts (D4/D5/D6)",
  "action": "SERIALIZED after g22-1 (advisor wave fix — same bee.mjs + registry file; worker rebases onto the g22-1 merge commit first). templates/bee.mjs doctor section: doctorOverallStatus -> three states: 'blocked' when any MECHANICAL blocking row not-ok; 'degraded' when mechanical ok but trust rows structurally unknown (re-class the 4 rows from blocking to degraded-class with degraded_reason naming /hooks); 'ready' = mechanical green + VALID attestation. New verb doctor.attest (registry + handler): records {hooks_file_sha256, codex_version, session_id, at, repo_identity} to a gitignored runtime-tier file. D5-REVISED validity (advisor R4 — the observation leg is CLAUDE-ONLY because hooks.jsonl is deny/crash-only and tools.jsonl is claude-only): codex VALID = current .codex/hooks.json sha matches + current codex --version matches + repo identity unchanged (static attest; the reason string says honestly that codex exposes no hook-fire event surface, so liveness is not attestable); claude VALID additionally requires one bee tool/hook event with ts > attestation.at in .bee/logs/tools.jsonl. Any failed leg -> attestation inert with the stale reason named -> degraded, never ready. D6: version-scope trust wording — probed-version constant (0.144.4); live codex --version differs -> rows say unprobed_version (suggest re-probe), never blanket unsupported. Fixtures in test_conformance.mjs + test_bee_cli.mjs: blocked / degraded / ready(static attest on codex) / stale (hash flip) / version-mismatch wording / claude observation leg. Mirrors + manifest.",
  "must_haves": {
    "truths": [
      "three states each proven by fixture: blocked / degraded / ready",
      "ready requires a valid attestation incl. a post-attestation observed event; any of the four validity legs failing -> degraded with the stale reason named",
      "trust verdict wording is version-scoped: live version != probed version -> unprobed_version, never blanket unsupported",
      "attest verb is registry-complete (bijection + example) and writes only gitignored runtime-tier state",
      "mirrors + manifest green"
    ]
  },
  "verify": "node scripts/test_conformance.mjs && node skills/bee-hive/templates/tests/test_bee_cli.mjs && node scripts/test_lib_mirror.mjs && node scripts/release_manifest.mjs --check",
  "files": [
    "skills/bee-hive/templates/bee.mjs",
    "skills/bee-hive/templates/lib/command-registry.mjs",
    "scripts/test_conformance.mjs",
    "skills/bee-hive/templates/tests/test_bee_cli.mjs",
    ".bee/bin/*",
    ".claude/skills/bee-hive/templates/*",
    ".agents/skills/bee-hive/templates/*"
  ],
  "status": "capped",
  "deps": [
    "g22-1"
  ],
  "decisions": [],
  "read_first": [],
  "trace": {
    "worker": "g22-3-worker",
    "outcome": "Doctor is three-state with codex ready actually reachable via static attest (hash+version+identity, honest no-liveness reason); trust rows degrade instead of block; verdicts version-scoped (unprobed_version); attest verb additive-routed; gitignore coverage side-fixed",
    "files_changed": [
      "skills/bee-hive/templates/bee.mjs",
      "skills/bee-hive/templates/lib/command-registry.mjs",
      "skills/bee-hive/templates/tests/test_bee_cli.mjs",
      "scripts/test_conformance.mjs",
      "skills/bee-hive/scripts/onboard_bee.mjs"
    ],
    "deviations": [],
    "friction": null,
    "capped_at": "2026-07-19T00:04:30.409Z",
    "behavior_change": true,
    "verification_evidence": "# g22-3 — done report (doctor three-state + attest + version scoping)\n\nWorker: g22-3-worker (sonnet, isolated worktree, rebased onto e784665).\nWorker commit `b4b489e`, merged into main (post-merge re-render + manifest\nregen). 2026-07-19.\n\n## What shipped\n- **D4**: `doctorOverallStatus` → ready/degraded/blocked. Mechanical rows\n  (hooks file, baseline match, handlers, skills, claude wiring rows) are\n  blocking → `blocked`. Codex's 4 trust rows re-classed `degrades:true` +\n  degraded_reason → hold `degraded`, cleared only by a valid attestation.\n  Claude: mechanical green = ready (no trust rows; documented).\n- **D5-REVISED**: `bee doctor attest --runtime codex` (additive routing via\n  the existing longest-prefix match — `bee doctor --runtime X` byte-identical\n  to before). Record {hooks_file_sha256, codex_version, session_id, at,\n  repo_identity} in gitignored `.bee/doctor-attest.json`. Validity = hash +\n  version + realpath identity; any drift → inert with named reason\n  (hash_changed/version_changed/identity_changed/no_attestation). No liveness\n  leg on codex — stated honestly in the reason text.\n- **D6**: PROBED_CODEX_VERSION='0.144.4'; live version parsed from\n  `codex --version`; mismatch → trust rows carry `unprobed_version:` prefix\n  instead of asserting probed conclusions.\n- Expectation updates (all named in the worker report): ok-fixture codex\n  not_ready→degraded; trust rows blocking→degrades; --json enum; conformance\n  scenario-12 clean→degraded / drifted→blocked (+ a missing sidecar the old\n  fixture masked). New tests: valid attest→ready; stale hash→degraded;\n  attest on claude refused; missing hooks file→blocked; unprobed-version\n  wording via PATH-stubbed fake codex.\n- Side fix: `.bee/doctor-attest.json` added to the onboarding gitignore block\n  (it had no coverage anywhere) + repo .gitignore.\n- 6-way mirrors; manifest regenerated.\n\n## Verification (fresh command output)\nIndependent orchestrator re-run on main post-merge (with re-render + manifest\nregen, plus cross-checks against the sibling g22-2 merge):\nconformance ALL PASS; bee_cli 185/0; lib_mirror 4/4; manifest 362 match;\nmodel_guard ALL PASS; dispatch_prepare 17/17 → G223_VERIFY_GREEN.\n\n## Diff\n30 files changed, 2276 insertions(+), 352 deletions(-).\n",
    "verify_output": "Independent re-run on main post-merge: conformance ALL PASS; bee_cli 185/0; lib_mirror 4/4; manifest 362 match; cross-checks model_guard ALL PASS + dispatch_prepare 17/17. Worker commit b4b489e.",
    "verify_passed": true,
    "claimed_at": "2026-07-18T23:44:34.061Z",
    "verify_command": "node scripts/test_conformance.mjs && node skills/bee-hive/templates/tests/test_bee_cli.mjs && node scripts/test_lib_mirror.mjs && node scripts/release_manifest.mjs --check",
    "verified_at": "2026-07-19T00:04:30.356Z"
  }
}
