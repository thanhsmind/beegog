{
  "id": "wsr-1",
  "feature": "worktree-session-routing",
  "lane": "high-risk",
  "behavior_change": true,
  "title": "bee worktree new --feature <slug>: create+register a feature worktree in one move (D7, GH #21)",
  "action": "Add createFeatureWorktree(mainRoot, {feature, baseRef}) to skills/bee-hive/templates/lib/worktree-store.mjs: validate slug against allowlist ^[a-z0-9][a-z0-9-]*$ and validate baseRef via git check-ref-format (advisor R2); every git call through spawnSync argv arrays (never a shell string) with -- terminator before user-derived values; typed zero-mutation refusals for non-ordinary checkout (resolveRoots worktreeResolution !== 'ordinary', mirroring register's linked-valid gate at bee.mjs:2097), existing sibling path, existing branch wt/<slug>, existing grant — AND catch git's own atomic failure as the real guard, typed (advisor R5); run git worktree add ../<repo-basename>--wt--<slug> -b wt/<slug> [baseRef]; derive the grant id by reading back the worktree's .git gitdir pointer after add, never assume id === dirname (advisor R3); then writeGrant + bootstrapWorktreeStore exactly as register does; return {id, worktreeRoot, branch}. On post-add failure: best-effort git worktree remove --force of the just-added tree + typed partial-state error naming the path; if rollback fails, say the tree is adoptable via register (advisor R3, test the injected-failure path). CLI verb worktree.new: registry entry (parameters.type='object' valid schema + deprecated key) + handler + UPDATE worktreeUsageFallback to 'Use: register, list, unregister, new.' — the bijection test regex-parses that string (anchors report coupling #1); prints created path + 'open your next session there'. Tests: extend scripts/test_worktree_cli.mjs e2e (create -> list shows grant -> store bootstrapped idle -> repeat call refuses -> bad slug/injection slugs refuse -> from linked worktree refuses -> injected post-add failure rolls back typed; all refusals zero-mutation) + bijection row in test_bee_cli.mjs. Sync byte mirrors: .bee/bin(+lib), .claude/skills/bee-hive/templates(+lib), .agents/skills/bee-hive/templates(+lib).",
  "must_haves": {
    "truths": [
      "worktree new from an ordinary checkout creates ../<basename>--wt--<slug> on branch wt/<slug>, grants it, and bootstraps an idle store (worktree list shows the grant)",
      "every refusal path (bad slug, existing path, existing branch, existing grant, linked-worktree caller) is typed and provably zero-mutation in the e2e fixture",
      "a failure after git worktree add reports a typed partial-state error naming what was created",
      "the printed output names the created path and tells the human to open the next session there",
      "registry+example bijection row exists and all four byte-mirror trees are identical"
    ]
  },
  "verify": "node scripts/test_worktree_cli.mjs && node scripts/test_worktree_store.mjs && node skills/bee-hive/templates/tests/test_bee_cli.mjs && node scripts/test_lib_mirror.mjs",
  "files": [
    "skills/bee-hive/templates/lib/worktree-store.mjs",
    "skills/bee-hive/templates/lib/command-registry.mjs",
    "skills/bee-hive/templates/bee.mjs",
    "skills/bee-hive/templates/tests/test_bee_cli.mjs",
    "scripts/test_worktree_cli.mjs",
    ".bee/bin/*",
    ".claude/skills/bee-hive/templates/*",
    ".agents/skills/bee-hive/templates/*"
  ],
  "status": "capped",
  "deps": [],
  "decisions": [],
  "read_first": [],
  "trace": {
    "worker": "wsr1-worker",
    "outcome": "bee worktree new shipped: createFeatureWorktree with typed zero-mutation refusals + post-add rollback, CLI verb + registry + usage-fallback bijection coupling, 4-way mirrors; independent verify green on integration branch (merge 3c56b7a, worker d2ef77a)",
    "files_changed": [
      "skills/bee-hive/templates/lib/worktree-store.mjs",
      "skills/bee-hive/templates/lib/command-registry.mjs",
      "skills/bee-hive/templates/bee.mjs",
      "skills/bee-hive/templates/tests/test_bee_cli.mjs",
      "scripts/test_worktree_cli.mjs"
    ],
    "deviations": [],
    "friction": null,
    "capped_at": "2026-07-18T10:25:25.965Z",
    "behavior_change": true,
    "verification_evidence": "# wsr-1 — done report (bee worktree new)\n\nWorker: wsr1-worker (sonnet, isolated harness worktree). Worker commit `d2ef77a`,\nmerged into integration branch `wt/worktree-session-routing` as `3c56b7a`\n(registered worktree `beegog--wt--worktree-session-routing`). 2026-07-18.\n\n## What shipped\n- `createFeatureWorktree(mainRoot, {feature, baseRef})` in\n  `skills/bee-hive/templates/lib/worktree-store.mjs`: spawnSync argv arrays with\n  `--` before user-derived values; typed zero-mutation refusals\n  (`WORKTREE_INVALID_SLUG`, `WORKTREE_INVALID_BASE_REF`,\n  `WORKTREE_CALLER_NOT_ORDINARY`, `WORKTREE_TARGET_EXISTS`,\n  `WORKTREE_BRANCH_EXISTS`, `WORKTREE_GRANT_EXISTS`); runtime add failure typed\n  `WORKTREE_ADD_FAILED`; post-add failure rolls back (worktree+branch+grant) as\n  `WORKTREE_POST_ADD_FAILED`, rollback failure `WORKTREE_POST_ADD_ROLLBACK_FAILED`\n  naming `worktree register` as the adoption path; grant id read back from the\n  new worktree's `.git` gitdir pointer (advisor R2/R3/R5 honored).\n- CLI verb `worktree new`: handler (requires resolveRoots 'ordinary'), handler\n  map entry, registry entry with example, `worktreeUsageFallback` = \"Use:\n  register, list, unregister, new.\" (bijection coupling honored).\n- 4-way byte mirrors synced (.bee/bin, .claude, .agents).\n\n## Verification (fresh command output)\nIndependent orchestrator re-run in the integration worktree after merge 3c56b7a:\n\n```\nnode scripts/test_worktree_cli.mjs && node scripts/test_worktree_store.mjs \\\n&& node skills/bee-hive/templates/tests/test_bee_cli.mjs && node scripts/test_lib_mirror.mjs\n...\n169 passed, 0 failed\nPASS test_lib_mirror: internal self-test — checker correctly flags injected byte-diff, missing file, and extra file\nPASS test_lib_mirror: runtime hook inventory derives launchers/imports and catches missing, extra, and byte drift\nPASS test_lib_mirror: templates/lib and .bee/bin/lib are byte-identical (19 files)\nPASS test_lib_mirror: runtime-derived hook inventory is byte-identical (10 files)\n```\n\nWorker-side full run (its own worktree, pre-merge): test_worktree_cli 49/49,\ntest_worktree_store 22/22, test_bee_cli 169/0, test_lib_mirror all PASS.\n\n## Diff (merge 3c56b7a)\n16 files changed, 1420 insertions(+), 8 deletions(-) — worktree-store.mjs +219,\ntest_bee_cli.mjs +50, plus CLI/registry edits and their three mirror trees.\n\n## Deviations (accepted)\n- Rollback also deletes the just-created branch (`-D`, only after confirmed\n  worktree removal) — required for the zero-mutation guarantee on injected\n  post-add failure.\n- Extra `worktree.new` example-execution test in test_bee_cli.mjs — forced by\n  the existing \"every registry example is executed\" guard.\n- Worker briefly created a stray probe worktree in shared repo metadata during\n  exploration; removed immediately, `git worktree list` confirmed clean.\n",
    "verify_output": "Independent orchestrator re-run in integration worktree (branch wt/worktree-session-routing, merge commit 3c56b7a): ... 169 passed, 0 failed; PASS test_lib_mirror: templates/lib and .bee/bin/lib are byte-identical (19 files); PASS runtime-derived hook inventory byte-identical (10 files). Worker-side full run: worktree_cli 49/49, worktree_store 22/22, bee_cli 169/0, mirror PASS. Worker commit d2ef77a.",
    "verify_passed": true,
    "claimed_at": "2026-07-18T10:05:18.015Z",
    "verify_command": "node scripts/test_worktree_cli.mjs && node scripts/test_worktree_store.mjs && node skills/bee-hive/templates/tests/test_bee_cli.mjs && node scripts/test_lib_mirror.mjs",
    "verified_at": "2026-07-18T10:24:33.239Z"
  }
}
