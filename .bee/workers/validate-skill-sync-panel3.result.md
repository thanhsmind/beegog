ITERATION 3: 0 still open, 2 new

F1 — CLOSED: “`--force-downgrade` overrides ONLY a refusal in which all three versions resolved numeric”; rollback: “`unknown`/`blocked_no_source` is never forceable.”

F3 — CLOSED: “SOURCE unknown … -> refuse and not forceable.”

F5 — CLOSED: Risk map: “no target parameter exists”; plan: “no override”; Security: “no production override exists.”

F7 — CLOSED: Slices now enumerate `skill-sync-0` through `skill-sync-3`, with isolation first and safety proof in cell 1.

F11 — CLOSED: Brief: “isolation lands FIRST as its own cell” and “safety-critical cases ride the implementation cell, then the matrix cell”; cell 2: “No changes to onboard_bee.mjs in this cell.”

NEW-1 — CLOSED: “PER-CASE opt-in assertions … never a suite-wide invariant — later cells intentionally write skills into their case homes.”

NEW-2 — CLOSED: Cell 1 requires a “NESTED symlink” byte-identity test and ancestor overlap “proven with zero mutations.”

NEW-3 — BLOCKER: Brief Security says “an older or unidentifiable source cannot mutate anything without an explicit `--force-downgrade`,” implying unknown may be forceable. Must say force applies only to resolved numeric downgrades.

NEW-4 — BLOCKER: Rollback says reverting “`skill-sync-1..3` restores the pre-feature script and suite,” but cell 0 changes the suite. Include cell 0 or narrow the claim.