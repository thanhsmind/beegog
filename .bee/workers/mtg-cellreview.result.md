OUTCOME: done

CELL REVIEW REPORT
Cells reviewed: 4
CRITICAL FLAGS: model-tier-guard-1 fail-open behavior is internally inconsistent / the action's stated flow reaches denial for `tool_name: Agent` with missing `tool_input`, while `must_haves` requires missing `tool_input` to exit 0 silently; the verify never exercises this case / add an explicit early-return rule for absent or non-object `tool_input` and a verify case proving exit 0 with empty stderr.
CRITICAL FLAGS: model-tier-guard-1 payload feasibility remains assumed / plan.md says `tool_name`, `tool_input.model`, `prompt`, and `description` must be confirmed against a live hook log, but the cell cites only sibling-hook shapes and its verify uses synthetic stdin / attach accepted live payload evidence before implementation, or make a bounded precondition/spike that proves the current Agent/Task payload shape.
CRITICAL FLAGS: model-tier-guard-1 verification does not cover its acceptance surface / the command checks bare, model, prompt marker, wrong tool, and junk JSON, but omits description marker, 500-character boundary, missing input, disabled config, no-repo behavior, case variants beyond one sample, stderr silence, and the configured-model FIX text required by the cell and plan / expand the payload-table verify to cover every must-have and the plan's boundary cases.
CRITICAL FLAGS: model-tier-guard-3 verification can pass with materially wrong documentation / it proves only that `bee-tier` occurs in seven files; it does not prove generation is the aux default, ceiling requires a marker plus justification, budget transport is documented, or existing review-slot/tier-judgment rules remain intact / add focused assertions for the required clauses and a diff/content check protecting the unchanged rules.
CRITICAL FLAGS: model-tier-guard-4 declares an incomplete write scope / `files` omits `docs/decisions/0015-ceiling-is-the-session-model.md` and `.bee/decisions.jsonl` even though the action and artifacts require both to change; onboarding also writes generated repo/global targets not accounted for by the cell / add every repo mutation to `files`, explicitly classify generated/global sync outputs, and ensure reservation handling covers the declared repo paths.
CRITICAL FLAGS: model-tier-guard-4 calls a synthetic pipe a live-fire check / plan.md requires a real denied dispatch after vendoring to validate the actual matcher and payload fields, while the action and verify only pipe handcrafted JSON directly into the hook / add an actual Agent/Task PreToolUse UAT step with captured denial evidence, or move that explicit requirement to a separately owned Gate 4 UAT artifact.
CRITICAL FLAGS: model-tier-guard-4 verification omits required durable outputs and preservation checks / the command does not verify the 0015 amendment, the decisions.jsonl entry, 0023's scoped wording, onboarding `recheck: up_to_date`, non-bee settings preservation, or installed-skill hash sync / snapshot the protected settings before apply and extend verification with exact doc/log assertions plus parsed onboarding recheck and sync evidence.
MINOR FLAGS: none.
CLEAN CELLS: model-tier-guard-2.
SUMMARY: All four verify commands are shell-syntax valid; no quoting or escaping defect was found. Cell 2 is cold-pickup ready, but cells 1, 3, and 4 need critical repairs because their current proof surfaces can pass without establishing locked behavior, and cell 4 also cannot be safely reserved from its declared file list.
