[DONE]

Outcome: Added a complete, inert, strict-JSON Bee configuration sample covering all nine supported top-level surfaces and safe examples of every supported model-slot shape.

Files touched: `config-sample.json` and this report; `.bee/config.json` was not changed.

Verification: passed the cell's exact Node structure assertions and confirmed no diff to `.bee/config.json`.

Cell trace: [config-sample-1](../../../../.bee/cells/config-sample-1.json)

Parent next action: collect the capped cell and continue to the review gate.
