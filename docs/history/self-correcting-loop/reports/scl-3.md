# scl-3 — judge-standard matrix: authoring advisory + behavior-class cap teeth (D3)

[DONE] — advisory `JUDGE_STANDARD_INSUFFICIENT` warning shipped at `cells add`/`cells update` (STDERR only, never a refusal), with mechanical cap teeth for the `behavior` class only: `capCell` refuses when `verification_evidence.red_failure_evidence` is absent, under 80 chars, or byte-identical to another cell's recorded evidence, naming the failed minimum / colliding cell id. F5 exception-door passthrough and an F5 STDERR cap-time advisory both included. 21 new test rows added; 4 pre-existing fixtures updated to clear the new floor. Full verify chain green; deliberate-red proof recorded and reverted clean.

Files touched: see `.bee/cells/scl-3.json` `trace.files_changed` for the full list (lib/bee.mjs source + `.bee/bin` mirrors, test_lib.mjs, test_bee_cli.mjs, release-manifest.json, `.claude-plugin`/`.codex-plugin` skill-tree projections).

Full trace/evidence: `.bee/cells/scl-3.json`.
