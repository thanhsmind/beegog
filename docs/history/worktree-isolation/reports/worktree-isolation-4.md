[DONE] Added attested transactional merge-back, committed-main provenance,
non-destructive revert, conservative cleanup, and explicit destructive-drop
recovery contracts, backed by deterministic temporary-Git fault injection.

Files touched: `skills/bee-swarming/references/swarming-reference.md`,
`skills/bee-hive/templates/tests/test_lib.mjs`.

Full verification evidence and output: [cell trace](../../../../.bee/cells/worktree-isolation-4.json).

The exact repository verification passed and the frozen judge reported no hits.
The required commit could not be created because this Codex checkout exposes
`.git` read-only (`.git/index.lock: Read-only file system`).
