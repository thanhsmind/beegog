{
  "schema": "judge-verdict/1",
  "verdict": "PASS",
  "checks": [
    {
      "id": "d23-split-not-weakened",
      "status": "PASS",
      "evidence": "test_hook_contracts.mjs:2731-2737 uses raw preTail === preControlTail; :2765-2769 uses preControlLead === preNudgeExpected against bytes from the fixture own vendored compaction.mjs. Judge mutations on a throwaway copy: MUT1 (delete the PreCompact nudge push) -> BOTH rows FAIL; MUT2 (one-word paraphrase VERBATIM->verbatim) -> row b FAIL; MUT3 (extra part on the anchor path only) -> row a FAIL. No substring or regex used for either equality."
    },
    {
      "id": "d23-tail-non-vacuous",
      "status": "PASS",
      "evidence": "test_hook_contracts.mjs:2736 asserts the tail carries the hive-door warning. MUT4 (suppress that warning so both tails go empty) -> row a FAIL with controlTail= withTail=null. The non-vacuity guard bites."
    },
    {
      "id": "d23-red-was-by-design",
      "status": "PASS",
      "evidence": "Judge reproduced the red rather than trusting the trace: pre-split test_hook_contracts.mjs from 8743c28~1 run against post-wiring hooks gives exactly one failure, precompact-no-anchor-output-is-untouched-anchor-is-additive, 193 rows 1 failing — matching the recorded evidence verbatim. Post-split is 194 rows: net +1, nothing removed."
    },
    {
      "id": "precompact-never-marks-injected",
      "status": "PASS",
      "evidence": "bee-session-close.mjs:318-325 gates shouldInject AND markInjected behind if(!force). test_compaction_advisories.mjs:344-363 runs the real PreCompact hook, asserts shouldInject still true, then runs the real UserPromptSubmit hook and asserts the nudge still fires. MUT5 (move markInjected outside the guard) -> that row FAIL, 6 passed 1 failed."
    },
    {
      "id": "no-turn-control-verdict-on-precompact",
      "status": "PASS",
      "evidence": "Sole encodeBlock call site is bee-session-close.mjs:472, guarded by maybeBypassBlock whose first statement is if(!ctx || ctx.event !== Stop) return null (:385-387). adapter.mjs:363-372 emitHookOutput can only emit encodeAdvisory or plain text — no decision field on that path. A missing hook_event_name yields ctx.event === empty string which also fails the Stop guard. Unreachable under any input."
    },
    {
      "id": "positive-control-is-real",
      "status": "PASS",
      "evidence": "test_compaction_advisories.mjs:269-276 asserts the same bypass fixture DOES emit decision:block on Stop. MUT7 (short-circuit maybeBypassBlock to return null) -> that row FAIL with the message the bypass fixture must block on Stop, otherwise the PreCompact row proves nothing. The row discriminates the event, not the fixture."
    },
    {
      "id": "survival-warning-off-fresh-record",
      "status": "PASS",
      "evidence": "bee-session-close.mjs:311-315 calls survivalWarning on appendCompactionRecord RETURN VALUE, never a re-read. compaction.mjs:277-290 makes the count self-inclusive with threshold 2 at :66, so a stale pre-append read would give 1 on the second compaction and null the warning, reddening test_compaction_advisories.mjs:301-306. The row discriminates the exact off-by-one by construction."
    },
    {
      "id": "sessionstart-rows-untouched",
      "status": "PASS",
      "evidence": "git show 8743c28 on that file returns exactly two hunks: the node:url import and @@ -2689,25 +2689,91 @@. Lines 2427-2455, 2740-2780 and 2854-2883 all sit outside it."
    },
    {
      "id": "dedup-placement-judgment",
      "status": "PASS",
      "evidence": "Per-hook placement is right for a reason the worker did NOT give: bee.mjs:2429 calls anchorMissing inside the read-only compact-check sweep, and both suites call it as a side-effect-free oracle. Folding markInjected into compaction.mjs would make a reporting command silently consume the 30-minute nudge budget — a direct violation of D13 reports-never-repairs — and would make the tests mutate the cache they measure. Not a consolidation candidate."
    }
  ],
  "fixability": "automatic",
  "confidence": "high"
}
