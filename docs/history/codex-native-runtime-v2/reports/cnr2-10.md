# cnr2-10 — Tag runtime passages (hive/swarming/executing) + D10 advisor transport

**[DONE]** — high-risk, capped 2026-07-18. Commit `1e5f6a2`.

## Outcome (one line)
Tagged runtime-specific prose across bee-hive/bee-swarming/bee-executing and their named references with the cnr2-9 markers by who-must-act, split the 3-column spawn table into paired per-runtime tables, and applied the one D10 semantic delta (advisor transport) — every claude projection is codex-mechanic-clean and every codex projection is claude-mechanic-clean, with marker-strip reproducing the frozen baseline modulo the permitted re-layout + D10.

## What changed (attribution by who-must-act)
- **Claude-only:** native worktree dispatch section (bee-swarming/SKILL.md + swarming-reference.md), the pinned `subagent_type`/`.claude/agents`/`bee-model-guard` guidance, the Claude spawn-mechanics table, and the model-shaped advisor clause carrying the `claude -p` fallback.
- **Codex-only:** the `wait_agent`/`list_agents` native-tending paragraphs (bee-swarming/SKILL.md + routing-and-contracts.md "Native Codex subagent tending" + swarming-reference.md "Native Codex timeout interval"), the Codex spawn-mechanics table, and the `subagent_type`-asymmetry note.
- **Restructuring (reshape decision d):** the 3-column "Runtime Spawn Mechanics" table → paired per-runtime 2-column tables; the interwoven step-3 spawn bullet → shared lead + paired per-runtime blocks + shared tail.
- **D10 (only semantic delta), bee-executing/SKILL.md Transport:** Claude projection keeps the Agent-tool + `claude -p` fallback; Codex projection gets a runtime-native model-shaped transport — a failed transport surfaces in the Consults section, never a silent cross-vendor CLI fallback unless the advisor slot itself is that CLI. cli-shaped advisor guidance stays shared.
- **bee-hive/SKILL.md:** untouched — no runtime-mechanic prose to attribute (marker-strip == baseline, zero diff).

## Render ownership
Sources tagged only. This cell did NOT write managed roots or plugin trees and did NOT touch the census (`test_lib.mjs` flip) — those belong to cnr2-11, the sole final-render owner. `test_plugin_distribution`'s drift-pin is EXPECTED red until cnr2-11 re-renders; this cell's verify deliberately excludes that suite (serialized by design).

## Verification
`node scripts/test_skill_render.mjs && node scripts/census_stale_spawn_syntax.mjs && echo TAG-OK` → test_skill_render 27/0, census clean, TAG-OK. Scratch render/grep + marker-strip-vs-frozen-baseline proof recorded in the cell trace; per-hunk diff justification and the red→green before/after leak evidence live in `.bee/cells/cnr2-10.json`.

Full trace/evidence: `.bee/cells/cnr2-10.json`.
