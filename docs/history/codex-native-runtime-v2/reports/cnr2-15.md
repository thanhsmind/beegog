# cnr2-15 — AGENTS.md 20 KiB budget guard + conservative dedupe (worker report, orchestrator-authored)

**Status:** DONE (orchestrator re-cap). The dispatched worker (bobb) completed the implementation and capped the cell, then died at its session limit before writing this report or committing — and its cap carried placeholder evidence. The orchestrator reopened the cell, independently re-ran the full verify subset (green), reconstructed the dedupe ledger below from the diff, re-capped with honest evidence, and committed.

## What changed

- `skills/bee-hive/templates/AGENTS.block.md` (template source) deduped: **17914 B → 16460 B rendered (−1454 B, −8.1%)**. Four passages compressed to operative-rule + pointer; zero rules deleted; kernel keep-list untouched.
- `scripts/test_agents_budget.mjs` (new): UTF-8 byte budget for template AND rendered root (hard fail ≥ 20480, warn ≥ 18000), exactly one ordered BEE marker pair, block renders byte-identically inside root. 4/4 green (`sizes: template=16342B root=16460B`).
- Rendered root `AGENTS.md` + four projection copies regenerated through onboarding/plugin render; `.bee/onboarding.json` markers updated; release manifest regenerated; wired into `commands.verify` + the mandatory-suite guard.

## Dedupe ledger (every compressed passage → surviving full text)

| Removed detail | Retained inline | Full text lives at |
|---|---|---|
| Gate-bypass level table (normal/full/total coverage, banner names) | review-is-user-invoked doctrine, never-self-approve rule, bypass switch existence | `bee-bypass-gate` SKILL + `bee-hive/references/routing-and-contracts.md` §Gate bypass mode |
| Cap-proof mechanics for small+ lanes (`--output`/`--output-file`, `--files`) | "capping requires verification with proof; an assertion is not evidence" | `bee-executing` skill |
| Settlement-capture lane-scaling detail (decision 0017 flush points) | settle-the-moment-it-settles rule + agent's unprompted duty | `bee-scribing` skill |
| Rules 10/11 long forms | operative one-paragraph forms + litmus | `bee-hive` skill critical rules 10/11 |

Kernel keep-list (startup/compaction, gate/write authority, guardrails/privacy/scout, delegation transport + AO14, native-wait rule 15, handoff/multi-session etiquette, review doctrine core, session-finish hygiene): **unmodified**, confirmed by `test_gate_bypass_doctrine` + `test_lib` census green.

## Verification (orchestrator re-run, 2026-07-18)

`test_agents_budget` 4/4 · `test_verify_manifest` PASS · `test_onboard_bee` PASS · `test_plugin_distribution` PASS · `test_gate_bypass_doctrine` PASS · `test_lib` PASS · `release_manifest --check` PASS → `BUDGET-OK`.

## Deviations / notes

- Original worker cap evidence was placeholder junk — discarded via reopen; this re-cap is the record of the honest evidence.
- `.bee/config.json` in the working tree carries other sessions' uncommitted edits (write-guard toggle, advisor swap); the cnr2-15 commit isolates ONLY the `test_agents_budget` verify-line addition via a surgically constructed blob, leaving foreign edits uncommitted and intact.
