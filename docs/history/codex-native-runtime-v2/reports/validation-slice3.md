# Validation report — codex-native-runtime-v2, slice 3 (S5 adapter split), rev4

Date: 2026-07-18. Lane: high-risk. Cells: cnr2-9 → cnr2-12 → cnr2-10 → cnr2-11 (serialized).

## Iteration history (the reshape earned its ceremony)

- **rev1** shaped inline-block renderer + 3 cells. Panel (review tier): **ITERATE** — 2 blockers (3-column runtime table + interwoven bullets cannot be line-filtered; verify greps vacuous AND unsatisfiable), 1 critical (self_skip premise false — real mechanism `applySyncSkill` mode "sync", onboard_bee.mjs:1162-1243), 3 warnings (two hash sites :693/:1233, verify-chain wiring claims, freeze-net not encoded). Advisor: **RETURN-TO-PLANNING** — 2 blockers (rendered projections accepted as lossy onboarding sources via source-identity.mjs:65; plugin routes ship raw source → cross-runtime bleed defeats D9), 5 majors (three integrity contracts; census test_lib.mjs:7879; marker grammar/atomicity; frozen-baseline equality proof; who-must-act attribution).
- **rev2** folded everything; advisor: **RETURN-TO-PLANNING** — 5 residuals (plugin topology still delegated; parallel tagging cells sharing rendered trees; own-runtime provenance ambiguity; verify token/phrase inconsistency; plan.md drift).
- **rev3** locked topology (committed rendered plugin trees; package-time rendering rejected), serialized tagging, simplified provenance to any-target refusal, reworded verify rules, synced plan.md; advisor: **RETURN-TO-PLANNING** — 2 textual contradictions left in cell prose.
- **rev4** removed both (own-runtime refusal explicit + no-exception prohibition; cnr2-10 scratch-render only, census flip and final render moved to cnr2-11). Advisor: **PROCEED, residuals: none.**

## Reality gate

MODE FIT PASS (same 5 flags; distribution machinery = highest blast radius in repo). REPO FIT PASS (panel verified: sync path, both hash sites, plugin-distribution fixtures self-contained, no missing runtime-prose files — go-mode.md does not exist; only census-excluded CREATION-LOG stray). ASSUMPTIONS PASS after two disproofs (self_skip premise; union-equality proof replaced by frozen-baseline diff). SMALLER PATH PASS (tagging without renderer = status quo bleed; renderer without plugin trees = advisor blocker 2). PROOF SURFACE PASS (baselines green; each cell's verify runnable; full chain wired by cnr2-9).

## Advisor consults

Four consults recorded (digests s3, s3r2, s3r3, s3r4; bundles alongside). Final recorded `advisor_ref`: digest-s3r4 (PROCEED). All findings folded into cells before dispatch — constraints-before-dispatch.

## Verdict

**READY.** Gate 3 auto-approved (bypass=total), audit decision logged. Dispatch order: cnr2-9 (ceiling) → cnr2-12 → cnr2-10 → cnr2-11.
