# cnr2-11 — Tag validating/reviewing + FINAL render + full suite

**[DONE]** — high-risk, capped 2026-07-18. Commit `308a692`.

## Outcome (one line)
Tagged bee-validating's plan-checker/cell-reviewer spawn mechanics with the cnr2-9 markers (bee-reviewing needed zero tagging — its reviewer-wave spawn prose was already runtime-neutral); flipped the native-Codex-empty-wait census in `test_lib.mjs` to be runtime-aware; added the render-model section to `docs/06-runtime-integration.md`; performed the sole final render of all four D9 rendered-tree roots and regenerated the release manifest to match; full 24-suite recorded verify chain green.

## What changed
- **`skills/bee-validating/SKILL.md`:** two passages split by who-must-act. Plan Checker section — shared lead (review-slot dispatch) + Claude-only sentence (`subagent_type: "bee-review"` spawn, `.claude/agents/bee-review.md`, `bee-model-guard` denial) + Codex-only sentence (no per-agent subagent type, read-budget + output-cap enforcement) + shared tail. Cell Review section — same split pattern (shared lead, Claude-only "same `bee-review` subagent_type" note, shared rhetorical question). Both hunks preserve every original word in original order; the only non-marker changes are one semicolon→period (closing the Claude-only sentence before the Codex-only one) and the minimal copula needed to turn a bare appositive phrase into a standalone Claude-only sentence.
- **`skills/bee-reviewing/SKILL.md`:** no edit. Grepped for every D9 attribution token used elsewhere in this feature (Claude, Codex, subagent_type, general-purpose, bee-model-guard, spawn_agent, wait_agent, list_agents, fork_turns, ORCH-0, followup_task, send_input, ceiling) across `SKILL.md` and `references/reviewing-reference.md`: the only hits are already-neutral shared prose ("spawn every reviewer as the runtime's default/general subagent type", "default `opus` on Claude" as a config-default fact, "GPT via codex CLI" as a cli-shaped-slot example) — data mentions, not who-must-act mechanics. Pre-tag blob unchanged.
- **Census flip (`skills/bee-hive/templates/tests/test_lib.mjs`):** split the single `procedureSurfaces` array into `procedureSurfacesKeepWaitAgent` (canonical sources + the newly-added `.agents/skills` Codex-rendered copies — must still carry wait_agent/list_agents) and `procedureSurfacesStripWaitAgent` (`.claude/skills` Claude-rendered copies — must now be free of wait_agent/list_agents and of any surviving `bee:only`/`bee:end` marker). Before D9 tagging + render, all copies were byte-identical passthroughs; after cnr2-10 tagged the native-tending prose Codex-only, the Claude projection genuinely strips it, so the census must assert absence there instead of presence.
- **`docs/06-runtime-integration.md`:** new "Render model: one source tree, runtime-conditional blocks (D9)" section — marker grammar, the four rendered-tree roots and their generators, provenance-sidecar refusal, and where cross-runtime contrast notes belong when tagging would otherwise displace them.
- **Final render (sole owner):** ran `scripts/render_plugin_skill_trees.mjs` (regenerates `.claude-plugin/skills/` and `.codex-plugin/skills/`) and `onboard_bee.mjs --repo-root . --apply` (regenerates the `.claude/skills/` and `.agents/skills/` managed roots), picking up all of cnr2-9/cnr2-10's canonical changes that had never been synced. Then `scripts/release_manifest.mjs --write` to re-baseline the release manifest against the freshly-rendered plugin trees — cnr2-10's report explicitly flagged this drift-pin as EXPECTED red until this cell re-rendered; `--check`/`--selftest` are green after the write.

## Verification
Per-cell verify (`TAG2-OK`): `test_skill_render` 27/0, `test_lib` 347/0 (includes the new census-flip absence assertions), `test_verify_manifest` PASS, `runtime-conditional` found in the doc. Full recorded verify chain (24 suites, `BEE_AGENT_NAME` unset) green end-to-end after the release-manifest write (first full-chain attempt correctly caught the expected-red `test_plugin_distribution` sha256 mismatch cnr2-10's report predicted; fixed by `release_manifest.mjs --write`; second attempt exits 0, zero FAIL lines). Spot-checked rendered output directly (not just the test suite): `.claude/skills/bee-swarming/SKILL.md` has 0 occurrences of `wait_agent`, `.agents/skills/bee-swarming/SKILL.md` has 1; same pattern confirmed on `routing-and-contracts.md` and both plugin trees. Full trace, evidence, and deviation record: `.bee/cells/cnr2-11.json`.

## Notes
- This cell's `onboard_bee.mjs --apply` run was the first managed-root sync since cnr2-9 (renderer machinery) and cnr2-10 (tagging) landed in canonical `skills/`, so the sync also caught up unrelated-but-overdue drift in `.claude/settings.json`/`.bak` (hook matcher superset, bee-tools-logger entry) and `.bee/onboarding.json` (hash bump) — disclosed here since it crossed slightly wider than the tagging diff itself, but it is the correct and required side effect of running the sync tool, not a scope creep.
- Left `.bee/backlog.jsonl`, `docs/specs/hook-runtime.md`, and `docs/specs/reading-map.md` untouched in the working tree — those are other concurrently-running cells' in-flight edits in this shared checkout, not this cell's to commit.
