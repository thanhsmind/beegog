# cnr2-13 — bee doctor --runtime codex|claude: fail-closed runtime health report

[DONE]

Added a `doctor` command group to `bee.mjs` (D11): `bee doctor --runtime codex|claude [--json]`.
Never reports "ready" from file presence alone. On `--runtime codex`, the four
structurally-unknown rows (`hooks_discovered`, `hooks_trusted`, `project_trust`,
`pending_hook_review`) are always present, `unknown`, and marked `blocking`, so
`overall_status` stays `not_ready` on this repo/codex-cli version regardless of
every other row (capability matrix row F1). `--runtime claude` has no blocking
rows and reaches `ready` on a fully-wired fixture. `checkManifestDrift`'s
pre-routing manifest-hash cache write is bypassed entirely for the `doctor`
route (proven by an unwritable-cache fixture test) and made best-effort
(try/catch) for every other command, so drift detection itself is unweakened.

Files touched: canonical `skills/bee-hive/templates/bee.mjs` and
`skills/bee-hive/templates/lib/command-registry.mjs` (hand-edited), plus
`skills/bee-hive/templates/tests/test_bee_cli.mjs` (10 new doctor fixture
tests + 2 existing group-allowlist tests updated for the new dot-free
`doctor` entry). Every mirror/projection copy (`.bee/bin/*`, `.claude/skills`,
`.agents/skills`, `.claude-plugin/skills`, `.codex-plugin/skills`) was
regenerated via `node skills/bee-hive/scripts/onboard_bee.mjs --repo-root . --apply`
and `node scripts/render_plugin_skill_trees.mjs`, then
`node scripts/release_manifest.mjs --write` regenerated
`docs/history/codex-harness-hardening/release-manifest.json`.

Full trace/evidence: `.bee/cells/cnr2-13.json`.
