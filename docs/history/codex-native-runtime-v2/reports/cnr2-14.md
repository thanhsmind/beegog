# cnr2-14 — Conformance suite: automatable subset + manual checklist

[DONE]

Added `scripts/test_conformance.mjs` (D12): a fixture-driven black-box
conformance suite over the automatable subset of `docs/REFs/be-codex.md`'s
P2 12-scenario list — scenario 3 (source write before Gate 3 denied by the
write-guard binary), scenario 6 (verify-red never caps), scenario 5-part-a
(reservation conflict names the holder), and scenario 12 (doctor
fail-closed on a drifted fixture) — plus two checks labeled ADAPTER
REGRESSIONS, not numbered scenarios: the state-sync `update_plan` matcher
superset across every rendered hook target, and the codex `spawn_agent`
guard's bare-denied/anchored-allowed/unobserved-fail-open tri-state. Every
check subprocess-drives the real public entrypoints (`.bee/bin/bee.mjs`,
`.bee/bin/hooks/bee-write-guard.mjs`, `.bee/bin/hooks/bee-model-guard.mjs`)
against an isolated fixture store built fresh per scenario — never the live
repo — and asserts the negative state (nothing written / cell stays
uncapped / no reservation granted to the loser), not merely a deny code.

Wrote `docs/history/codex-native-runtime-v2/conformance-checklist.md`: the
manual protocol for the 9 interactive/agent-behavior scenarios (1, 2, 4,
5-part-b, 7, 8, 9, 10, 11), each with setup, observable pass condition, and
the P2 metric(s) it feeds (all 10 named metrics covered).

Wired `scripts/test_conformance.mjs` into `.bee/config.json`
`commands.verify` and into the mandatory-suite guard
(`scripts/test_verify_manifest.mjs`'s `MANDATORY_SUITES`); regenerated
`docs/history/codex-harness-hardening/release-manifest.json`.

Files touched: `scripts/test_conformance.mjs`,
`docs/history/codex-native-runtime-v2/conformance-checklist.md`,
`.bee/config.json`, `scripts/test_verify_manifest.mjs`,
`docs/history/codex-harness-hardening/release-manifest.json`.

Full trace/evidence: `.bee/cells/cnr2-14.json`.
