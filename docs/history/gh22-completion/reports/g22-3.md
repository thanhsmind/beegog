# g22-3 — done report (doctor three-state + attest + version scoping)

Worker: g22-3-worker (sonnet, isolated worktree, rebased onto e784665).
Worker commit `b4b489e`, merged into main (post-merge re-render + manifest
regen). 2026-07-19.

## What shipped
- **D4**: `doctorOverallStatus` → ready/degraded/blocked. Mechanical rows
  (hooks file, baseline match, handlers, skills, claude wiring rows) are
  blocking → `blocked`. Codex's 4 trust rows re-classed `degrades:true` +
  degraded_reason → hold `degraded`, cleared only by a valid attestation.
  Claude: mechanical green = ready (no trust rows; documented).
- **D5-REVISED**: `bee doctor attest --runtime codex` (additive routing via
  the existing longest-prefix match — `bee doctor --runtime X` byte-identical
  to before). Record {hooks_file_sha256, codex_version, session_id, at,
  repo_identity} in gitignored `.bee/doctor-attest.json`. Validity = hash +
  version + realpath identity; any drift → inert with named reason
  (hash_changed/version_changed/identity_changed/no_attestation). No liveness
  leg on codex — stated honestly in the reason text.
- **D6**: PROBED_CODEX_VERSION='0.144.4'; live version parsed from
  `codex --version`; mismatch → trust rows carry `unprobed_version:` prefix
  instead of asserting probed conclusions.
- Expectation updates (all named in the worker report): ok-fixture codex
  not_ready→degraded; trust rows blocking→degrades; --json enum; conformance
  scenario-12 clean→degraded / drifted→blocked (+ a missing sidecar the old
  fixture masked). New tests: valid attest→ready; stale hash→degraded;
  attest on claude refused; missing hooks file→blocked; unprobed-version
  wording via PATH-stubbed fake codex.
- Side fix: `.bee/doctor-attest.json` added to the onboarding gitignore block
  (it had no coverage anywhere) + repo .gitignore.
- 6-way mirrors; manifest regenerated.

## Verification (fresh command output)
Independent orchestrator re-run on main post-merge (with re-render + manifest
regen, plus cross-checks against the sibling g22-2 merge):
conformance ALL PASS; bee_cli 185/0; lib_mirror 4/4; manifest 362 match;
model_guard ALL PASS; dispatch_prepare 17/17 → G223_VERIFY_GREEN.

## Diff
30 files changed, 2276 insertions(+), 352 deletions(-).
