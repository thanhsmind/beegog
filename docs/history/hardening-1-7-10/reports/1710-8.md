# 1710-8 — nested-clone origin-rewrite hunt

Status: DONE (verify green). Files: `scripts/test_nested_clone_isolation.mjs`.
Full trace/evidence: `.bee/cells/1710-8.json`.

Outcome: no script-level culprit found despite an exhaustive grep (zero
`remote`/`set-url` hits anywhere in scripts/skills/.bee/bin/hooks) and full
empirical reproduction (all named candidate suites plus a full 53-suite
`run_verify.mjs` run from a fixture nested clone) — none corrupted a parent
fixture's `.git/config`. The leftover `.bee/spikes/clean-clone/.git/config`
in this repo (origin = the real checkout's own path) is the actual incident
artifact; its creation timing points to a procedural cwd-persistence mistake
during manual release-flow git setup, not a code defect. Added
`scripts/test_nested_clone_isolation.mjs` as a regression tripwire pinning
the real invariant (resolveRoots / createFeatureWorktree / mergeFeatureWorktree
/ a cwd-pinned `bee status` never touch a parent's remotes from a nested
clone) instead of a fabricated culprit.
