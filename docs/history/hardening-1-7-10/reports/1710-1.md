# 1710-1 — CI green: windows.yml split suites, session-env hermeticity, deterministic claim_race barrier, root-skip chmod

**Status:** [DONE]

**Outcome:** windows.yml no longer references the deleted `test_lib.mjs`; it
now runs every `skills/bee-hive/templates/tests/` suite via a new
`BEE_VERIFY_ROOT_FILTER` env on `run_verify.mjs`. `run_verify.mjs` and both
`test_state.mjs`/`test_claims.mjs` scrub `CLAUDE_CODE_SESSION_ID`/
`BEE_SESSION_ID` so local runs match CI. Fixed the one hermetic-only failure
this surfaced in `test_state.mjs`'s lane-overlap reservation check.
`test_claim_race.mjs`'s (g-unsafe) negative control now uses a deterministic
fs-based barrier instead of a sleep, proven 10/10 across 20 consecutive runs.
`test_cells.mjs`'s chmod-0555 simulation now skips loudly under a root euid.

**Files touched:**
- `.github/workflows/windows.yml`
- `scripts/run_verify.mjs`
- `scripts/test_claim_race.mjs`
- `skills/bee-hive/templates/tests/test_state.mjs`
- `skills/bee-hive/templates/tests/test_claims.mjs`
- `skills/bee-hive/templates/tests/test_cells.mjs`

**Full trace/evidence:** `.bee/cells/1710-1.json`

**Note:** a full `node scripts/run_verify.mjs` run shows one unrelated
pre-existing failure in `skills/bee-hive/scripts/test_plugin_distribution.mjs`
(release-manifest sha256 drift), caused by sibling in-flight cells
(1710-2/1710-6) editing `lock.mjs`/`hooks/catalog.mjs` ahead of the 1710-9
close-out render+manifest step. Out of this cell's scope — none of the 6
files above touch plugin skill tree rendering.
