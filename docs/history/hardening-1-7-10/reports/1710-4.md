# 1710-4 — report

**Status:** [DONE]

**Outcome:** writeCell now serializes against the `cells-archive` lock (sync,
single-attempt; typed `CELLS_ARCHIVE_BUSY` on contention, `CELL_ARCHIVED`
instead of resurrecting an archived-only id); `reopenCell`/`setTier`/
`resetCellBudget`/`recordJudgeVerdict` gained the same `assertNotArchived`
guard the other six mutators already had; `archiveFeature`/`unarchiveFeature`
now preflight destination collisions before any rename, journal the planned
moves before the first rename, and recover an interrupted prior transaction's
journal at entry; `unarchiveFeature` refuses to overwrite an existing active
file; fixed the false serialization comment on `archiveFeature`'s lock.

**Files touched:**
- `skills/bee-hive/templates/lib/cells.mjs`
- `skills/bee-hive/templates/lib/lock.mjs`
- `skills/bee-hive/templates/tests/test_cells.mjs`

Full trace/evidence: `.bee/cells/1710-4.json`.
