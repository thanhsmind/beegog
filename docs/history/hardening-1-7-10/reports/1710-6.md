# 1710-6 — win-hook-root

**Status:** [DONE]

**Outcome:** `commandWindows` (hooks/catalog.mjs's `commandWindowsFor`, onboard_bee.mjs's
`codexHookCommandWindows`) is now a shell-agnostic `node -e SCRIPT <script>` bootstrap that
resolves the real git root via `git rev-parse --show-toplevel` before requiring the hook, fixing
MODULE_NOT_FOUND from any nested session cwd. `hooks/test_hook_contracts.mjs`'s
`codex-repo-commandWindows-contract` row now asserts the exact structural shape plus a
forbidden-character check (`$`, `%`, backtick), and a new required row
`codex-commandWindows-nested-cwd-execution` actually executes the live rendered
`commandWindows` string against a real git fixture from both a nested subdirectory and a
non-git sibling directory. `.codex/hooks.json` regenerated through the existing catalog
render path (not hand-edited).

**Files touched:** `hooks/catalog.mjs`, `skills/bee-hive/scripts/onboard_bee.mjs`,
`hooks/test_hook_contracts.mjs`, `.codex/hooks.json`

**Commit:** cc79d8b — `fix(hooks): commandWindows resolves git root via shell-agnostic node -e bootstrap [1710-6]`

**Verify:** `node hooks/test_hook_contracts.mjs` → 180 rows, 0 failing, ALL PASS.

Full trace and verification evidence: `.bee/cells/1710-6.json`.

Note: the full-chain `node scripts/run_verify.mjs` shows unrelated failures (plugin skill tree
mirror drift on `templates/tests/test_state.mjs`) from concurrent sibling cells (1710-1/1710-2)
still mid-flight in this swarm — outside this cell's file scope; not a regression from this cell.
D9 close-out owns the final full-chain green re-check.
