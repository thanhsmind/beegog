# 1710-7 — judge-lifecycle (D7)

[DONE] — Judge NEEDS_REVISION now reopens a capped cell to `open` (not
`claimed`) with claim + verify evidence cleared via `releaseTrace`, closing
the stale-evidence re-cap hole; the claims-store is reconciled defensively on
the same transition. `dispatch-prepare.mjs`'s rendered prompt now carries the
validated worker name as the reservation identity; `--force-ownership` is now
explicitly advisory-only (`ownership_override.transferred: false`) in its
returned envelope.

Files touched: `skills/bee-hive/templates/lib/cells.mjs`,
`skills/bee-hive/templates/lib/dispatch-prepare.mjs`,
`skills/bee-hive/templates/tests/test_misc.mjs`.

Full trace/evidence: `.bee/cells/1710-7.json`.

Friction filed: dispatch-prepare `--force-ownership` has no simple transfer
primitive on the `trace.worker` ownership axis (P3, layer `cells-lifecycle`,
`.bee/backlog.jsonl`).

Note: `.bee/bin/lib/{cells,dispatch-prepare}.mjs` were deliberately left
unsynced with the templates (plan.md D9: sync at close-out) — the
`test_misc.mjs` vendored-source byte-identity row stays red mid-swarm as a
result; confirmed out of scope by testing (a premature sync broke the live
CLI via a sibling cell's unmirrored `lock.mjs` export) and reverting cleanly.
