# rel1710rc-3 — [BLOCKED]: cross-session claim override mid-task

**Outcome:** Root-caused and fixed two real races (`claims.mjs` GATE_HELD orphan,
`test_claim_race.mjs` scenario (b) timing flake). Before the store_lock (f)/(c)
fix could be completed, this cell's claim was forcibly overridden by another
live session (agent `w-rc3`, session `83c38091-b589-48e1-b368-1a9f95892969`,
`ownership_overrides` entry in the cell trace, `owner_bypassed` = this
session). That session has since actively reworked `test_store_lock.mjs` and
`test_worktree_holds_race.mjs` under its own still-open reservations. The
write-guard hook hard-blocked further writes here (D3 cross-session hold), so
work stopped per AGENTS.md rule 14 — no retry, no edit-around.

## Files touched (before the override; still in the shared working tree)

- `.bee/bin/lib/claims.mjs` — added bounded retry (`acquireGateWithRetry`,
  15 attempts x 20ms) around the claim-release gate. Root cause: a losing
  budget-exhausted racer's own `releaseClaim` unwind could collide with
  ANOTHER racer's concurrent release on the same transient `.adopting` gate
  file, get a single-shot `GATE_HELD`, and never retry — permanently
  orphaning its own just-created claim file. Reproduced under
  `taskset -c 0,1` stress (1 orphan in 160 runs before the fix, 0 in 160
  after). Mirrored automatically to `skills/bee-hive/templates/lib/claims.mjs`.
- `scripts/test_claim_race.mjs` — scenario (b)'s fixed `UNSAFE_WIDEN_MS`
  sleep is a scheduler-timing-dependent negative control (same class as the
  rel1710rc-2 fix elsewhere in this codebase); replaced with the same
  deterministic fs-barrier technique already used by this file's own
  (g-unsafe) control. Reproduced "DETECTOR DID NOT BITE" in >30% of stress
  runs before the fix, 0/160 after.

Full trace/evidence: `.bee/cells/rel1710rc-3.json` (trace.worker is now
`orch` / claim_session `83c38091-...`, not this session — cap authority no
longer held here).

## Not completed

- `scripts/test_store_lock.mjs` scenario (f)/(c) crash-vs-refusal fix — the
  other live session has since reworked this file directly (171 lines
  changed, same counted-refusal design). A same-run solo verification (read
  -only, no repo write) of its CURRENT in-flight state showed it is not yet
  green: one run hit `(c) ... 3 mutual-exclusion violation(s)`, another hit
  `(f) locked racer produced no parseable {completed,refused} summary: null`
  — both under a single unstressed solo invocation. This may reflect
  mid-edit state on their side; not this session's code to fix now.
- Full `run_verify.mjs` not run to completion (deferred — the store_lock
  suite above is red under the other session's current in-flight edit).

## Outstanding Questions

- Which session (this one or `w-rc3`) should finish and cap rel1710rc-3?
  Recommend letting `w-rc3` finish (it holds the live claim + reservations)
  and, before it caps, having it re-run the store_lock proof protocol — the
  solo-run failures above suggest its edit isn't finished yet.
- Two throwaway local stress-test scripts this session created
  (`.bee/bin/.rel1710rc3_stress_debug.sh`, `.bee/bin/.rel1710rc3_stress_verify.sh`)
  could not be deleted (blocked by the same cross-session hold on
  `.bee/bin`). They are harmless (untracked dotfiles, `.sh` extension, not
  matched by `run_verify.mjs`'s `test_*.mjs` discovery glob) but should be
  removed by whoever finishes the cell, or directly by the user.
