# rel1710rc-2

[DONE] — Made scenario (c) in `test_worktree_holds_race.mjs` deterministic via an fs-based ready-file barrier (10/10 green, unconstrained and under `taskset -c 0,1`), and `windows.yml`'s windows-portable subset now honestly excludes exactly the 4 named suites with a P1-backlog-referenced annotated block.

Files touched:
- `scripts/test_worktree_holds_race.mjs`
- `.github/workflows/windows.yml`
- `scripts/run_verify.mjs` (deviation: auto-add — new `BEE_VERIFY_EXCLUDE` env var, needed to implement the windows.yml exclusion without a second hand-maintained suite list)

Commit: `05a131f` — "fix(ci): deterministic holds-race negative control + honest windows-portable subset [rel1710rc-2]"

Full trace/evidence: `.bee/cells/rel1710rc-2.json`
