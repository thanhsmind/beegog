# codex-hook-state-parity-3

[DONE]

One shared strict distribution engine now arbitrates plugin-first and repo-copy installs for Bash and PowerShell, proves enabled installed-package inventory or plugin inactivity before mutation, fences project and ledger-owned cleanup, preserves foreign hooks/skills, and applies from a revalidated whole-run snapshot. Plugin metadata validates, the canonical release inventory covers 136 files, and cachebuster testing stays in disposable staging.

Files touched: shared distribution helper and 22-case fixture; Bash/PowerShell installers; onboarding Codex projection; plugin/marketplace metadata; mandatory verify and release inventory machinery. BA spec synchronization is intentionally left to the parent scribing pass.

Verification: the literal cell command exited 0; frozen-scope judge reported zero hits. Full trace and evidence: [cell record](../../../../.bee/cells/codex-hook-state-parity-3.json).

Real install limitation: this worker did not mutate real user-home/plugin state. Windows PowerShell parsing could not be re-probed through WSL because the interop transport failed before PowerShell started; the repository's cross-platform ASCII guard and full fixture suite are green.
