# ih-3 — install.sh follows the new defaults

[DONE] — layer-1 global skills copy gated behind `--global-skills` (default off); `--no-claude-md` pass-through added; `--claude-md` kept as accepted no-op alias; usage/help/safety notes and header comment rewritten for the per-project default layout.

Files touched: `scripts/install.sh`

Full trace/evidence: `.bee/cells/ih-3.json`
