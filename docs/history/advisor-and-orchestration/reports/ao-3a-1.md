# ao-3a-1 — W4: passive tools logger

Worker: Jerry (generation tier) · Capped: 2026-07-17T14:56:10Z · Status: DONE

## What changed

`hooks/bee-tools-logger.mjs` — a new PostToolUse (no matcher = every tool, BOTH runtimes) wrapper, thin per the `bee-prompt-context.mjs` pattern: adapter → root → `hookEnabled(root,'tools-logger')` → appends one JSON line `{ts, tool_name, agent_id, agent_type}` to `.bee/logs/tools.jsonl` per AO15/decision `f1ca79b9` (`agent_id`/`agent_type` null on orchestrator calls; `duration_ms`/`status` appended only when the payload carries them). Zero enforcement: every throw is `logCrash` + exit 0, never stderr, never a decision, never `tool_input`/`tool_response` bodies.

`hooks/catalog.mjs` gained a second `PostToolUse` group (BOTH runtimes, no matcher) alongside the existing `state-sync` group. `hooks/hooks.json` and `hooks/claude-hooks.json` regenerated byte-exact via `renderProjectionText`. `.codex/hooks.json` (D9 hand-curated snapshot) hand-extended with only the tools-logger `PostToolUse` group, copying the shape of the existing state-sync group — never re-rendered. `.claude/settings.json` hand-added the matching `PostToolUse` block. `DEFAULT_HOOKS['tools-logger'] = true` added to both `skills/bee-hive/templates/lib/state.mjs` and its `.bee/bin/lib/state.mjs` mirror (byte-identical, `diff` verified).

**Two hardcoded counts fixed** (both are the real prior behavior this cell changes, both now bear a new commit's evidence): `hooks/test_hook_contracts.mjs:944` `repoCommands.length === 11 → 12` (the KNOWN RED named in the cell text) and `hooks/test_hook_contracts.mjs:2810` `commands.length === 9 → 10` (a sibling instance on the hand-curated `.codex/hooks.json`'s command count, found independently during implementation — same class, not called out in the cell text — fixed as an in-path deviation, `route-config-nine-commands` renamed `route-config-ten-commands`).

**Fails-when-broken pair** added to `hooks/test_hook_contracts.mjs`: `toolsLoggerRows()` (happy-path orchestrator + subagent rows — real fixture, real payload, asserts the emitted line's shape and that `tool_input`/`tool_response` never leak into it) and standalone `runToolsLoggerCrashRows()` (own dedicated fixture, `state.mjs` corrupted to throw on import per `test_model_guard.mjs`'s `buildThrowingStateFixture` pattern — asserts exit 0 AND a visible crash line in `hooks.jsonl`, never a silent `tools.jsonl` write). `bee-tools-logger.mjs` added to `WRAPPERS` so the 7 universal malformed-input rows auto-apply. 4 new `repo-route` arms for `PostToolUse:tools-logger` fell out automatically from `routeRequiredRowIds`, which derives from the live `.codex/hooks.json`.

## Files

`hooks/catalog.mjs`, `hooks/bee-tools-logger.mjs` (new), `.bee/bin/hooks/bee-tools-logger.mjs` (new, byte-identical mirror), `hooks/hooks.json`, `hooks/claude-hooks.json`, `.codex/hooks.json`, `.claude/settings.json`, `hooks/test_hook_contracts.mjs`, `skills/bee-hive/templates/lib/state.mjs`, `.bee/bin/lib/state.mjs`, `docs/history/codex-harness-hardening/release-manifest.json` (regenerated via `--write`, 143 files).

## Verification

`node hooks/test_hook_contracts.mjs && node hooks/test_model_guard.mjs && node hooks/test_write_guard.mjs && node skills/bee-hive/templates/tests/test_lib.mjs && node scripts/test_lib_mirror.mjs && node scripts/release_manifest.mjs --check` → **exit 0.** `test_hook_contracts.mjs`: 162 rows / 19 check groups, 0 failing (was 160 rows before this cell), ALL PASS. `test_model_guard.mjs`/`test_write_guard.mjs`: ALL PASS. `test_lib.mjs`: 342 passed, 0 failed. `test_lib_mirror.mjs`: templates/lib and `.bee/bin/lib` byte-identical (19 files); runtime-derived hook inventory byte-identical (10 files, was 9). `release_manifest --check`: 143 files match.

**Red-before evidence for the two count fixes:** temporarily reverted both assertions back to 11/9 and reran the suite — it printed `FAIL - catalog-drift :: codex-repo-target-generated-topology :: repo transport contract violated: commands=12 (expected 12)` and `FAIL - repo-route :: route-config-ten-commands :: expected 10 configured commands, found 10` (2 FAILURE(S)). Reverts undone, suite rerun green before capping.

Sample `tools.jsonl` lines from the happy-path fixture: `{"ts":"2026-07-17T14:52:49.420Z","tool_name":"ToolsLoggerHappyOrchestratorProbe","agent_id":null,"agent_type":null}` and `{"ts":"2026-07-17T14:52:49.453Z","tool_name":"ToolsLoggerHappySubagentProbe","agent_id":"agent-jerry-1","agent_type":"generation"}` — proving no `tool_input`/`tool_response` body reached the log.

Full trace/evidence: `.bee/cells/advisor-and-orchestration-ao-3a-1.json` (or the ID bee.mjs resolved it to — see `cells show --id ao-3a-1`).

## Friction — two out-of-scope regressions found, not fixed here

Two more hardcoded hook-command counts exist **outside this cell's bounded files** and will go stale/red the moment the full repo `commands.verify` chain runs, but editing them was outside the explicit prohibition ("no edits outside the bounded files"):

1. **`skills/bee-hive/scripts/test_onboard_bee.mjs:669`** (+ synced mirrors at `.claude/skills/bee-hive/scripts/test_onboard_bee.mjs:669` and `.agents/skills/bee-hive/scripts/test_onboard_bee.mjs:669`) — `check(codexCommandCount === 11, ...)` asserts the ONBOARDING-GENERATED `.codex/hooks.json` (a different artifact from the checked-in one this cell hand-edited) carries exactly 11 commands. Now stale at 12. **This WILL fail red** the next time `commands.verify` (or `test_onboard_bee.mjs` directly) runs — it is in the recorded verify chain.
2. **`scripts/test_installers_e2e.mjs:306-321`** — the `VENDORED_HOOKS` array (9 entries) and its surrounding "9 hooks" prose are missing `bee-tools-logger.mjs`. This does **not** turn the suite red (the check only asserts listed names exist, not that the directory has exactly N files), but it's a silent coverage gap — the new hook's vendoring goes unverified.

Recommend a tiny fix-first follow-up cell (same shape as this feature's own Slice 0 `ao-0a`/`ao-0`/`ao-0b` pattern) bumping `codexCommandCount` 11→12 in all three synced copies and adding `bee-tools-logger.mjs` to `VENDORED_HOOKS`, before the next full `commands.verify` run.

## Notes

- No cost-reduction claim anywhere (CONTEXT ban, W4 explicitly measures, does not reduce).
- Per-tool-call node spawn is precedented (write-guard fires per Edit/Bash/Read).
- `tools.jsonl` has no rotation, consistent with `hooks.jsonl`/`dispatch.jsonl`.
