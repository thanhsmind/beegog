# ao-4-1 — advisor_ref plumbing + Gate 3 high-risk precondition (AO3/AO13)

**[DONE]** — Worker: Otto · Lane: standard (ceiling-judged) · behavior_change: true

Shipped the orchestrator advisor path plumbing: an `advisor_ref` state field with
verb-stamped AO13 staleness anchors, the `state advisor-ref record`/`show` CLI
verbs, and the Gate 3 precondition that refuses high-risk execution approval
without a fresh advisor consult (throws, never warns; each AO13 reason named).

- **state.mjs**: `advisorRefAnchors(root, feature)` (feature / newest active decision id / `sha256(plan.md)`, missing plan → `ADVISOR_PLAN_ABSENT_SENTINEL`) and `advisorRefStale(root, ref, state)` (AO13 verbatim; malformed/missing ref reads as stale, never throws). Pure reads.
- **bee.mjs**: `handleStateAdvisorRefRecord` (anchors stamped by the verb, bound to the selected record's feature — checker M1; refuses with no active feature; stores `digest_head` = first 500 chars), `handleStateAdvisorRefShow`; `handleStateGate` precondition + `gate_revoked_at.execution` stamping on `execution --approved false`. Registered in the handler map and the state usage fallback (`advisor-ref`).
- **command-registry.mjs** (both copies, byte-identical — checker B1): `state.advisor-ref.record` + `state.advisor-ref.show` with schemas.
- **tests**: 14 new `test_bee_cli.mjs` rows (record refuse/stamp, show round-trip, Gate 3 throw/pass, each of the 4 AO13 conditions isolated, non-high-risk + other-gates untouched, malformed-ref-as-missing, example census); `test_lib.mjs` export census gains the two functions + the sentinel.
- Mirrors synced (`templates` ↔ `.bee/bin`, test files ↔ `.claude`/`.agents`); `release_manifest.mjs --write` regenerated (146 files).

**Verify** (green, chain exit 0): `test_bee_cli` 159/0 · `test_lib` 342/0 · `test_lib_mirror` PASS · `test_split_brain_regression` TARGET met · `release_manifest --check` 146 match.

Sample Gate 3 refusal:
> gate: execution approval refused for high-risk work — the advisor consult is missing or stale (AO3/AO13). Reason(s): no advisor_ref recorded. FIX: resolve the advisor from config (models.<runtime>.advisor), run it read-only with the evidence bundle on stdin, then record the consult: bee state advisor-ref record --advisor "<identity>" --digest-file <path>. Nothing is written until a non-stale advisor_ref exists.

Commit `b297317`. Full trace + verification evidence: `.bee/cells/ao-4-1.json`.

**Scope note (out of this cell's bounds):** the `.claude`/`.agents` runtime projections of `lib/state.mjs`, `lib/command-registry.mjs`, and `bee.mjs` were not in this cell's `files` list (only their test mirrors were) and were left untouched — they resync on the next onboarding `--apply`.
