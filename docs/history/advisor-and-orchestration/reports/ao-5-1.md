# ao-5-1 — AO14: tiny/small execute via a dispatched execution worker

**Status:** [DONE]

**Outcome:** bee-hive's lane table and tiny fast-path prose now name one dispatched execution worker for `tiny`/`small` Execute (never "direct, in-session (solo)"); bee-swarming's "Solo execution" section is rewritten to "Single execution worker" (registers in the swarm registry, takes reservations, single worker under the standard execution contract, no wave/reviewers/panels), with the orchestrator authoring the tiny/small done-report from the worker's verbatim diff plus its own verify re-run. The Delegation contract in `routing-and-contracts.md` gains the execution-worker class beside the I/O-offload worker (distinguished by authority and state effects). `AGENTS.block.md`'s critical rule 13 parenthetical carries the AO14 rider and root `AGENTS.md` was re-rendered via self-onboard (never hand-edited). Stale "solo execution" cross-refs in bee-swarming/bee-planning/bee-reviewing were updated. One new census row in `test_lib.mjs` pins the class and the retired phrasing. Release manifest regenerated.

**Files touched:** `skills/bee-hive/SKILL.md`, `skills/bee-swarming/SKILL.md`, `skills/bee-hive/references/routing-and-contracts.md`, `skills/bee-hive/templates/AGENTS.block.md`, `AGENTS.md`, `skills/bee-hive/templates/tests/test_lib.mjs`, `skills/bee-planning/SKILL.md`, `skills/bee-reviewing/SKILL.md`, plus their `.claude/skills/` and `.agents/skills/` mirrors, and `docs/history/codex-harness-hardening/release-manifest.json`.

**Verification:** full trace and evidence in `.bee/cells/ao-5-1.json`.

**Commit:** `c77e38d` — `feat(ao-5-1): AO14 — tiny/small execute via one dispatched execution worker`
