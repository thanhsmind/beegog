# ao-2aii-2

**Status:** DONE — capped, verify green, committed `f7a77a1`.

**Outcome:** The Delegation contract (`routing-and-contracts.md`) gained the cli gather branch: when `resolveTier(root, 'generation', runtime, {for:'gather'}).type === 'cli'`, a gather runs the configured command verbatim (nothing appended), prompt on stdin, every worker-facing path absolute, read-only by contract. Stdout **is** the digest, framed by a `<<<BEE_DIGEST … BEE_DIGEST>>>` delimiter contract; missing delimiters or an empty digest is a failed run, surfaced loudly, never a silent green. No `result.json`/cell/reservation/worker registration for a gather. The known `dispatch.jsonl` measurement gap (a Bash-launched gather emits zero rows) is named and handed to Slice 3. `AGENTS.block.md` critical rule 13 gained one added sentence (the cli-gather transport rider); root `AGENTS.md` was re-rendered via the repo's own self-onboard script to match — that run also synced this repo's own installed bee-hive mirrors (`.agents/skills`, `.claude/skills`) and `.bee/onboarding.json`'s managed-hash record, clearing drift left over from `ao-2aii-1`. Census anchors added to `test_lib.mjs`.

**Files touched:** `skills/bee-hive/references/routing-and-contracts.md`, `skills/bee-hive/templates/AGENTS.block.md`, `AGENTS.md`, `skills/bee-hive/templates/tests/test_lib.mjs`, `docs/history/codex-harness-hardening/release-manifest.json`, plus (deviation, see below) `.agents/skills/bee-hive/{references/routing-and-contracts.md,templates/AGENTS.block.md,templates/lib/state.mjs,templates/tests/test_lib.mjs}`, `.claude/skills/bee-hive/{references/routing-and-contracts.md,templates/AGENTS.block.md,templates/lib/state.mjs,templates/tests/test_lib.mjs}`, `.bee/onboarding.json`.

**RED anchor proof:** (1) before any edit, the real anchor strings (`BEE_DIGEST`, `cli gather branch`) were grepped as 0 matches in all three target files. (2) After writing the new census check but before re-rendering root `AGENTS.md`, `node skills/bee-hive/templates/tests/test_lib.mjs` failed exactly the new check (341 passed, 1 failed) with the message naming the missing rider on `AGENTS.md`. Running the self-onboard script re-rendered `AGENTS.md`; the suite went green (342 passed, 0 failed).

**Deviations:** the cell's `files` list did not name the `.agents/skills/bee-hive/*` / `.claude/skills/bee-hive/*` installed mirrors or `.bee/onboarding.json`, but the cell action explicitly mandates re-rendering `AGENTS.md` via the repo's own onboard script, and this repo self-hosts bee-hive. Running that mandated command mechanically synced the installed mirrors from the edited source templates (surfacing and clearing pre-existing drift in `templates/lib/state.mjs` left unsynced by `ao-2aii-1`) and updated `.bee/onboarding.json`'s managed-hash record. Included in this cell's commit as a direct, deterministic byproduct of the one instructed command — not an out-of-scope hand-edit. `docs/history/advisor-and-orchestration/plan.md` and `.bee/decisions.jsonl` also showed as modified in `git status` but predate this cell's work (prior gate-approval logging) and were deliberately left unstaged.

Full trace/evidence: `.bee/cells/ao-2aii-2.json`.
