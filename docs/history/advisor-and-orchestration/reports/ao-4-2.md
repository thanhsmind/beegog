# ao-4-2 — Gate 3 advisor-consult prose in bee-validating (AO2b/AO3/AO4)

**[DONE]** — Worker: Carl · Lane: standard · behavior_change: true · deps: ao-4-1 (capped)

Extended `skills/bee-validating/SKILL.md`'s Gate 3 section with the orchestrator
advisor-consult prose that ao-4-1's plumbing enforces at runtime: the consult
runs as machinery under every bypass level (never a new human checkpoint),
`resolveAdvisor` branches (cli-shaped → verbatim command + evidence bundle on
stdin; model-shaped → `bee-review`-class dispatch; unconfigured → record and
proceed), the `state advisor-ref record` verb, the throw-on-stale enforcement
with the four AO13 conditions stated verbatim, and advice-never-approves +
locked-decision supremacy (existing law, cited not restated). Added two
matching Red Flags rows.

- **skills/bee-validating/SKILL.md**: new prose block inside `## Gate 3 —
  Execution Approval`, before the machine-report line; two new Red Flags rows.
- Mirrors (`.claude/skills/bee-validating/SKILL.md`,
  `.agents/skills/bee-validating/SKILL.md`) synced byte-identical.
- `docs/history/codex-harness-hardening/release-manifest.json` regenerated via
  `node scripts/release_manifest.mjs --write` (146 files).

**Verify** (green, chain exit 0): `test_lib.mjs` 342/0 · `test_onboard_bee.mjs`
PASS (failures: 0, skipped: 1) · `release_manifest --check` 146 match. Note:
run the chain **without** `BEE_AGENT_NAME` set — two pre-existing
cross-session-hold rows in `test_lib.mjs` read that env var and fail when
present (confirmed by an unset/set A/B rerun); unrelated to this cell's scope.

Full trace + verification evidence (including the `git show
HEAD:skills/bee-validating/SKILL.md` before-state check): `.bee/cells/ao-4-2.json`.
