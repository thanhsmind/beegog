# Product Backlog

| ID | Story | CoS | Status | Feature |
|----|-------|-----|--------|---------|
| P43 | Installer upgrades never split the release version across Codex, Claude, runtime, or project skills | Both top-level installers fail closed on a mixed release tuple and prove greenfield plus brownfield end to end before reporting success; 1.3.1 released 2026-07-16 with the Linux half proven (Bash E2E 15 cases as mandatory verify suite; read-only pre-confirm, rollback, status-gated refusals; plugin-first tuple coverage; managed-set cleanup fencing) and all 9 hosts onboarded to 1.3.1; Windows/PowerShell E2E remains the open half (owner request 2026-07-16; decisions 55ff17ef, 09b776b5, fc76ce41) | in-flight | installer-version-parity-1-3-1 |
| P40 | Each swarm worker gets its own git worktree | Implementation and deterministic Git transaction/fault tests are green, but the required live native-worktree commit/merge acceptance remains blocked because the current Codex checkout mounts `.git` read-only. Complete the live attested commit, transactional merge-back, committed-main verify, and recovery-safe cleanup in a writable Git environment before marking done. | in-flight | worktree-isolation |
| P69 | Every bee skill reads and writes the knowledge bundle instead of loose markdown | bee-scribing writes concepts, bee-compounding promotes them, bee-hive assembles its context packet from `knowledge context`, bee-grooming hunts stale knowledge — and Claude, Codex and any other runtime consume one shared bundle through per-runtime adapters, so a handover needs only the Markdown bundle, not the bee runtime (deferred by okf-foundation D22; requires P65). **Delivered** (okf-switchover-f3, 2026-07-22, `docs/history/okf-switchover-f3/`): bee-scribing writes concepts through one bundle-mode predicate with a three-layer anti-fork gate (f3-2/f3-3); bee-hive's reading order flipped to bundle-first and `knowledge context` now ranks critical patterns by measured relevance rather than dumping all of them (f3-1/f3-4); `docs/specs/` fenced read-only for new content with structural stub recognition (f3-4); the profile itself migrated into the bundle (f3-5). **Remaining**: bee-compounding has no `knowledge promote` verb — promotion is still hand-authored (that verb is P67's own row); bee-grooming does not hunt stale knowledge (P68's row); per-runtime adapters for a bundle-only handover are unstarted. | in-flight | okf-switchover-f3 (partial — see Delivered/Remaining) |
| P60 | The behaviour corpus is language-neutral, so any implementation can be validated by the same tests | **Phase 0 of the Rust migration (decision `51d54157`, [strategy](decisions/0025-rust-migration-strategy.md)) and the gate on every later phase.** 35 of 56 suites already drive the CLI across a process boundary and validate any implementation as-is; the other 18 import JS modules directly and are the only structural lock-in. CoS: (1) those 18 suites are expressed as data — `(input, repo state) → expected verdict/output` — executed against whatever binary is under test; (2) no expectation encodes a JS internal (a module name, an export, an error class); (3) the full corpus runs green against today's JS runtime; (4) a deliberately broken build fails it (the corpus must bite). Valuable even if the migration is abandoned | proposed | — |
| P61 | Rust write-guard hook as a deferring fast path | **Phase 1.** Scoped in [CONTEXT](history/rust-write-guard/CONTEXT.md) (D1-D5). CoS: hook ≤5ms measured by the 10-run median method **against the import-trimmed JS baseline (~25ms), not today's 42ms**; the P60 corpus green against the binary; anything the binary does not fully recognize defers to JS; absent/incompatible binary ⇒ JS runs unchanged. Stop-condition from the strategy: if the real-session win is under ~15ms/call, the migration stops after hooks | proposed | — |
| P62 | Binaries ship reproducibly for five targets before the second port | **Phase 2 — the real cost of the migration, and it must exist before porting beyond the first hook.** bee ships today by copying files with no build step anywhere. CoS: CI cross-compiles linux x64/arm64, macOS x64/arm64, windows x64; checksums published and verified at install; the installer selects by platform and silently falls back to JS on no match; a tagged release proves all five plus the fallback | proposed | — |
| P63 | The runtime is Rust, one process at a time, JS deleted as it goes | **Phases 3-5**, in this order: remaining hooks → read-only verbs (status/list/show/search) → single-writer mutators → **concurrency layer last** (lock/claims/reservations/worktree-holds — three genuine correctness bugs surfaced there in one day, and its cost is I/O, not CPU). CoS per unit: corpus green, the unit's JS **deleted in the same feature** (no dual implementations — every drift incident this repo has had was two copies of one truth), store format unchanged in that step, and the phase independently revertible by deleting a binary | proposed | — |
| P59 | CLI reads land under 100ms — lazy module loading first, a Rust write-guard hook binary second | Trigger-conditioned (decision `2e224c90`, user follow-up 2026-07-21): (1) lazy-load the dispatcher so each verb imports only its own module slice — target simple reads ~30ms, status ~200ms; (2) if per-tool-call hook tax still hurts after that, port ONLY the write-guard hook to a small Rust binary (48-53ms → ~3ms per tool call), keeping node orchestration + copy-in distribution; full-CLI rust rewrite stays rejected while remaining cost is OS-bound I/O+git. Prove with the same 5-run median methodology as cli-performance | proposed | — |
| P41 | Write-time deny gains a wait/queue option | Today a file conflict is a hard deny and a `[BLOCKED]` worker idles until orchestrator rescue. Once computed waves (P39) land, measure whether real stalls remain; only then consider a bounded wait/queue primitive at the write guard (deferred from parallel-scheduler exploring D4, 2026-07-15) | proposed | — |
| P38 | Installer offers the status line as a one-flag opt-in | A real user installed bee and reported "I don't see the status line" — it is deliberately opt-in (decision 102efe08: onboarding never touches settings.json uninvited) and today enabling it is a manual 2-file copy + settings edit from `.claude/skills/bee-hive/templates/statusline/`. Add `--statusline` to install.sh/install.ps1 (and an onboard flag): with explicit consent it copies the pair to `~/.claude/` (or repo-level with the repo-scoped command) and merges the `statusLine` entry into settings.json with a `.bak`, same merge discipline as the hooks merge. Requires jq present — the flag should check and say so. Claude Code only (source: user report 2026-07-15) | proposed | — |
| P36 | Decision records carry a machine-readable claim, so conflicts become detectable | Prerequisite for the advisor's "two locked decisions in conflict" trigger (AO2(c), deferred by AO9 — not dropped). Today `decision`/`rationale` are free prose with no structured claim, `scope` is the default `"repo"` in 106 of 135 records and absent from every `supersede` event, and the store's only mechanical relation (`supersedes`) is the *resolution* of a conflict, not its detection — so two active decisions can never be in a machine-visible contradiction. Give records a subject/predicate/value claim. Needs its own exploring: it is a data-model change to an append-only log with 135 existing events (schema, migration, CLI shape, compatibility with `activeDecisions`/`supersede`) (owner decision 2026-07-14) | proposed | — |
| P33 | Turn-count discipline becomes an explicit rule, proven by the logger | Batching tool calls (one compound `Bash` beats five `Read`s) attacks the first-order cost term directly, but it is prose guidance and advisor-and-orchestration is deliberately about mechanism over exhortation — revisit once `tools.jsonl` can prove the effect on a real session (deferred from advisor-and-orchestration exploring, 2026-07-14) | proposed | — |
| P34 | Advisor thresholds are tuned from measured data, not guessed | "Two consecutive red verifies" is an invented number. Once the tools logger and advisor records run on real sessions, re-derive the trigger thresholds — and the false-positive rate of the decision-conflict detector — from what actually happened (deferred from advisor-and-orchestration exploring, 2026-07-14) | proposed | — |
| P35 | A session's bill is attributable to lanes and cells | A cost snapshot written at session close, so spend can be traced to the lane and cell that caused it instead of read off a status bar (deferred from advisor-and-orchestration exploring, 2026-07-14) | proposed | — |
| P29 | A headless outer loop runs validated cells through fresh sessions without a human at the keyboard | `while claim-next; do claude -p "execute <cell>"; done` — each cell gets a brand-new context; builds on fresh-session-handoff's cross-session claims and planned-next handoff; interactive flow must be proven first (deferred from fresh-session-handoff exploring, owner conversation 2026-07-13) | proposed | — |
| P25 | Bee can select Codex custom agent roles when the collaboration runtime exposes reliable role selection | Explorer, worker, and reviewer profiles are generated from one dispatch contract; swarming selects the intended profile per call, proves the effective model/sandbox/instructions, and detects unused or drifting profiles (deferred from codex-runtime-parity D4; re-verified NOT unlockable on codex-cli 0.144.4 — .codex/agents/*.toml not discovered, spawn_agent accepts built-ins only; capability matrix codex-native-runtime-v2 cnr2-4; re-probe on future Codex versions) | proposed | — |
| P47 | A claim wedged by a hard crash frees itself | The `.adopting` marker has no TTL/stale-reclaim: a SIGKILL mid-adoption wedges the claim permanently and the cell can never be claimed again without hand surgery. Independently corroborated by two reviewers in the v0.1.44 review (reliability + code-quality). Add a TTL/stale-sweep consistent with the existing claim heartbeat rules (source: review finding, promoted from machine backlog 2026-07-17) | proposed | — |
| P48 | Lane and session records stop being freely hand-writable gate inputs | Gate decisions read lane/session records that any process can hand-edit — a forged record can flip a gate or force compounding-complete. Decide the integrity mechanism (checksums, CLI-only writes like decisions.jsonl, or hook-guarded paths) and close the hole (source: v0.1.44 review finding, security + architecture, promoted from machine backlog 2026-07-17) | proposed | — |
| P50 | The two-terminal fresh-session handoff is proven by a real owner walkthrough | fresh-session-handoff (P28) shipped race-proven at the store level, but the real flow — two terminals, one `/clear`, planned-next adoption at the fresh boundary — has never been walked through by the owner. One recorded UAT session closes this (outstanding since v0.1.33, 2026-07-13) | proposed | — |
| P51 | Worktree feature-parallelism reaches the host projects | P44 shipped proven in beegog only; host rollout was explicitly D6-deferred and only anphabe-gogl is registered in dogfood_repos. Decide the rollout shape under the no-host-propagation directive (hosts pull via onboard themselves) and document the enable steps for a host owner (deferred from worktree-feature-parallelism D6, 2026-07-16) | proposed | — |
| P53 | The plan freeze is machine-enforced, not prose-ruled | Hash plan.md at Gate 2 approval; a hook denies post-approval content writes to it (approval stamp exempt). Deferred from lane-ceremony-v3 (D1 ships prose-only) | proposed | — |
| P54 | Cells carry an explicit slice id for long multi-slice features | `slice_id` on cells + slice-aware claim/status for standard/high-risk features that run many slices; v3 defines active slice as the open cells only (deferred from lane-ceremony-v3 D2) | proposed | — |
| P55 | Lane graduation is automated when evidence crosses a threshold | Mid-flight tiny→small→standard escalation beyond D8's re-run rule: mechanical trigger when file count or flags grow past the lane cap during execution (deferred from lane-ceremony-v3) | proposed | — |
| P67 | Finishing work promotes knowledge instead of accumulating it | `bee knowledge promote --work <id>` turns cell traces and `delivery.md` into updated Area/Feature concepts, new Decisions and new Patterns, and explicitly drops the temporary detail — so knowledge improves per session without growing per session (deferred by okf-foundation D22) | proposed | — |
| P68 | Stale, dangling, and self-contradicting knowledge is found by machine, not by memory | `bee knowledge stale` and `bee knowledge links` surface concepts whose area changed underneath them, dangling `required_context`/`supersedes` targets, and duplicate authority claims beyond what `check --strict` already catches (deferred by okf-foundation D22) | proposed | — |
| P56 | A crashed session's unsettled context is recovered from the harness transcript | Mid-run machine shutdown leaves no HANDOFF; post-crash resume rebuilds only from decisions/state/cells, so everything not yet settled into a decision is lost. Teach recovery to mine the harness-persisted session transcript (~/.claude/projects/*.jsonl) as a SECONDARY reconciliation source against decisions/state — decisions stay primary memory, raw transcripts are mined by a down-tier worker and never loaded as session context (source: user field report 2026-07-20; friction row filed same day). SHIPPED 2026-07-20: cells transcript-recovery-1..4 — recovery.mjs (detection + bounded mining window + down-tier miner prompt), `recovery scan|window` CLI + fail-open status block, `capture add --source mined` for unconfirmed mined settlements, test_recovery wired into the verify chain + bee-hive Session Scout offer paragraph; all 4 judged PASS, full verify green ([docs/history/transcript-recovery/](history/transcript-recovery/)) | done | transcript-recovery |
| P50 | A Codex session runs bee with the same experience as Claude Code | Truthful runtime docs with a three-state hook-trust model; state-sync fires on Codex's native plan tool; a pre-spawn dispatch guard on the observed Codex envelope (fail-open on unobserved shapes); runtime-clean skill prose via one canonical source + per-runtime rendered managed roots and committed plugin trees with provenance (rendered projections refused as onboarding sources); a fail-closed `bee doctor --runtime codex|claude`; a scripted conformance subset + manual checklist with named metrics; AGENTS.md under a hard 20 KiB budget with kernel intact; capability-gated throughout — custom agents and plugin-hooks verdicts version-scoped to codex-cli 0.144.4 (external review docs/REFs/be-codex.md, verified claim-by-claim) | done | codex-native-runtime-v2 |
| P32 | Bee knows when to ask a second opinion, and stops doing the work itself | Observable events — not felt uncertainty — trigger a cross-family advisor consult that blocks only at the close points (`cells cap`, Gate 3); a subagent's model becomes a structural property (pinned agent definitions) instead of something the caller must remember; cell execution always dispatches a worker so the orchestrator's window holds plans and digests, never source; and a passive tools logger finally measures the thing that costs money (the bill is turns × context, and `dispatch.jsonl` records only dispatches, never non-dispatches). Origin: a real host session billed $24 on ceiling against $0.41 across three subagents — under-delegation, not over-reading; the first diagnosis was overturned by an adversarial cross-family advisor, which is the feature's own argument for existing (owner conversation 2026-07-14; decisions D1–D6) | done ([docs/history/advisor-and-orchestration/](../docs/history/advisor-and-orchestration/)) | advisor-and-orchestration |
| P13 | Advisor mode is dogfooded end-to-end | Run one real feature on the generation tier with ceiling consults at shape/execution/blocked; confirm verdicts change decisions and the ceiling stays scarce (deferred debt of decision 0013) (advisor enabled in this repo 2026-07-10; done = one real feature run with recorded consults) | done: killed 2026-07-12 per fanout-delegation D1: advisor mode removed before dogfood completed; reversal logged | advisor-dogfood |
| P8 | Advisor pattern (cheap main loop, ceiling on demand) is first-class | `.bee/config.json` `advisor` (enabled + consult points) + `advisorModel` resolver; loud surfacing in preamble/bee_status; contract in routing-and-contracts, swarming rescue points to it (decision 0013) | done: advisor mode removed 2026-07-12 per fanout-delegation D1 | 0.1.11 |
| P64 | Bee's knowledge lives in one validated OKF bundle instead of five scattered document kinds | `docs/knowledge/` is a conformant OKF v0.1 bundle under a Bee Profile: nine namespaced types, stable `bee.id`, lifecycle, `required_context` links, generated per-level indexes (including the critical-pattern index). Ships `bee knowledge check|index|list|context`, the migrator, and `advisor-protocol` (202 lines) split end-to-end into concepts as the proof area, with a coverage report proving every numbered rule lands in exactly one concept. Legacy trees are retired by migration behind pointer stubs, never annotated in place; `.bee/` runtime untouched | done | okf-foundation (docs/history/okf-foundation/, bundle docs/knowledge/) |
| P46 | Git/VCS commands are exempt from the intake gate | A `git commit`/`git status` between features is bookkeeping, not source work — today the intake gate blocks git once phase is terminal, forcing workarounds. Decide the exemption's exact shape (read-only always allowed? commit allowed when staged paths are docs/.bee only?) and implement it in the write guard without reopening the door the gate exists to shut (deferred from GitHub #1, reporter discussion open since v1.3.10; registered here 2026-07-17 so it stops living only in a closed-issue thread). Per-clause evidence at flip (D11b): (1) shape decided — delivered, [CONTEXT.md](history/intake-gate-git-exemption/CONTEXT.md) D1/D3/D4 (read-only git always allowed, deliberately enumerated with fail-closed on an unrecognized subcommand; a mutating git command is exempt at a terminal phase only when every path it would actually change is bookkeeping — `.bee/`, `docs/`, `plans/`, `AGENTS.md`; `git push` never exempt; eligibility never inferred from a flag or commit message); (2) implemented in the write guard — delivered, `checkGitBashCommand` in `skills/bee-hive/templates/lib/guards.mjs` (mirrored to `.bee/bin/lib/guards.mjs`), wired into `hooks/bee-write-guard.mjs`, resolves real staged/target paths via `git diff --cached --name-only` at check time (D4); (3) door not reopened — delivered, scoped to terminal phases only (a staged source path outside a terminal phase is untouched by this logic), an unrecognized git subcommand fails closed, and a misleading `-m` message changes nothing. Proof: `hooks/test_write_guard.mjs` rows 47-56, full suite 147/147 green | done | intake-gate-git-exemption |
| P57 | A reversed decision propagates to every artifact that cites it, a backlog row only flips done when its CoS is actually met, and decisions stay findable at scale | (1) `decisions supersede` runs a propagation sweep over `docs/**` for the superseded id and refuses to finish silently — every hit is reconciled same-turn or becomes a capture stub; (2) done-flip enumerates the row's CoS clauses with cited evidence and never flips on a delivered subset (row keeps Delivered/Remaining annotation instead); (3) decide events accept `tags[]`/`area`, search gains `--tag/--area/--since/--all`, a CLI-rendered index lands under `docs/decisions/`, and an archive verb keeps the active store small — all proven by tests + one end-to-end supersede dry-run (GH #32/#33/#34). Per-clause evidence at flip (R7, `b9b9fee3`): (1) delivered — dp-2 sweep+stubs (f367f70) + live e2e `d20f4c96`→`257ab1e5` with reconcile/waiver ([e2e report](history/decision-propagation/reports/e2e-supersede.md)); (2) delivered — dp-8 scribing/compounding rule (e750945); (3) delivered — dp-1/3/4/5/6/7: tags+filters (91e3329), archive+locked appends (df76756), retro-tag overlay (97c12a6), taxonomy+ranked search (f822567), index render (34ce04d), 406-event backfill + index bootstrap (8a802ad, 746d51a); suite 84 checks, verify 55/55 | done | [decision-propagation](history/decision-propagation/) |
| P24 | Codex receives the same bee skills and lifecycle guardrails as Claude Code | The Codex plugin, installed skills, repo hook wiring, hook payload/output contracts, and active runtime docs stay versioned and tested in lockstep with Claude Code; Codex can bootstrap, guard writes/privacy, preserve state, and nudge the chain mechanically instead of relying only on AGENTS.md prose (source: owner request 2026-07-11) — residual doc-drift and guard gaps closed by codex-native-runtime-v2 (stale no-hooks claims retired, update_plan matcher superset, spawn_agent guard, doctor, conformance) | done | codex-runtime-parity |
| P49 | `--force-downgrade` names its blast radius before acting | Shipped: a forceable refused apply now carries `host_items` — the exact copy_lib/copy_helper paths under `.bee/bin/**` a force would overwrite, verbatim from the pending plan (present-empty when no runtime drift; omitted when unforceable). Spec R26; advisor-consulted; exact-equality three-step test (source: v1.1.0 review P2, promoted 2026-07-17; shipped 2026-07-18, cell p49-force-downgrade-blast-radius-1) | done | main (unreleased) |
| P52 | Small work starts from an executable work packet, not a shrunken feature plan | Lane-ceremony v3: plan.md frozen at Gate 2 (approval stamp only, no post-approval enrichment); current slice lives only in cells; tiny = request + cell (no plan.md), small = logged scoping synthesis + 1–3 cells (plan.md opt-in); merged shape+execution gate asked only after the exact work packet exists; lane file caps count product files only (never `.bee/**`, `docs/**`, plans/briefs/reports); the two over-broad risk flags narrowed to test-anchored wording; planning classifies cheaply first and scales bootstrap to the lane (source: user-supplied critique verified claim-by-claim 2026-07-19; CONTEXT D1–D10). SHIPPED 2026-07-19: cells lcv3-1..5, RED-first doctrine assertions (57-assert suite), AGENTS/README restated, plugin trees + manifest regenerated per-cell, full verify chain green recorded ([docs/history/lane-ceremony-v3/](history/lane-ceremony-v3/)) | done | lane-ceremony-v3 |
| P45 | Exploring asks fewer, batched rounds instead of one question per message | bee-exploring serialized every gray-area question one-per-message (a Hard Gate), so a set of independent product decisions cost N round-trips of user waiting. Revised (TDD-for-skills, RED→GREEN): a delegated step-3 pre-pass generates the candidate slate already tagged independent/dependent with dependency edges; step 4 batches all independent survivors into ONE AskUserQuestion message (tool supports 4) and serializes ONLY dependent/branching questions. Never blind-bundle (a question a prior answer could moot is dependent); materiality + gate-bypass info/approval split still run first. RED baseline: 3-indep+1-dep produced 4 serial rounds; GREEN verified: Scenario A → 2 rounds (batch{Q1,Q2,Q3}+conditional Q4), Scenario B genuine dep-chain → 3 rounds no batching (caveat holds). Full recorded verify green; manifest regen'd; mirrors byte-identical. CREATION-LOG amended 2026-07-16 (source: user report "bước đó chờ lâu quá" 2026-07-16) | done | exploring-batch-questions |
| P44 | A single session fans work into independent-feature git worktrees, each with its own gates, merged back as event-sourced state | New opt-in mode (coexists with P40, does not touch its D2): a worktree runs its own full bee lifecycle — own phase, own gates, own append-only `log/` — and reconciles both code and provenance to `main` on `git merge`. `.bee/` splits into three lifecycle tiers (`log/` tracked+`merge=union`, `cache/` derived-by-`bee replay`, `runtime/` live-never-merged); replay is idempotent (stable event id → sort → dedup). Same-feature/same-file concurrency stays out of scope (claims/holds). Subsumes P30. Proven in beegog first before any host rollout (source: mdview host report + owner direction 2026-07-16; docs/history/worktree-feature-parallelism/CONTEXT.md D1–D6). SHIPPED 2026-07-16: spike GREEN 5/5 → S2 decision module → S4 resolveRoots wire-in (P40 byte-for-byte, test-first) → Slice A `bee worktree register/list/unregister` CLI + bootstrap → Slice B tier merge-safety (.gitattributes merge=union + runtime/cache gitignore). 4 new test suites in commands.verify; full suite green; pushed (1011b52→ffc2b03). Host rollout still deferred (D6). | done | worktree-feature-parallelism |
| P30 | Tracked append-only .jsonl records merge without conflicts across branches | `merge=union` gitattributes for `.bee/*.jsonl` tracked files (decisions, backlog, review-candidates) so parallel branches/worktrees stop producing trivial merge conflicts (deferred from fresh-session-handoff exploring; delivered by P44 Slice B — .gitattributes gives decisions/backlog/review-candidates jsonl the union merge driver) | done | worktree-feature-parallelism |
| P42 | Codex subagent waits do not spam empty completion panels | After a native agent wait times out with no completion, the orchestrator never calls `wait_agent` again immediately. It continues material task-local work or, when none remains, takes exactly one `list_agents` snapshot; then it posts a concise update naming live state and the next action before any later bounded wait. Timeout never means agent failure, never releases ownership, and never triggers duplicate dispatch. Source: user screenshot 2026-07-15 showing repeated `Waiting for agents` / `No agents completed yet`. | done | [codex-agent-wait-loop](history/codex-agent-wait-loop/) |
| P39 | Parallel cells stop politely waiting: the scheduler computes waves from declared files+deps | User report (2026-07-15): swarm cells wait on each other more than they work. Root causes confirmed by code walk: `files` never checked for cross-cell overlap at plan time, no dep-graph/cycle pass, wave grouping is manual orchestrator judgment, conflicts surface only as write-time DENY with no queue — a mis-partitioned plan serializes at execution instead of being re-shaped at planning. Give bee a computed schedule: overlap matrix + dep graph over declared cells → cycle refusal at `cells add`, auto-computed waves (overlapping cells auto-serialize into later waves), surfaced via CLI for swarming/validating (compare: Van's task-intake harness — acyclic store, refuse-on-write; bead_rust_viewer plan-splitting) (source: user report via owner, 2026-07-15; backlog friction P2 same day) (shipped 2026-07-15: schedule.mjs computeSchedule/detectCycles, cycle refusal at add/update, `bee cells schedule` verb, swarming/validating/planning prose — docs/history/parallel-scheduler/) | done | parallel-scheduler |
| P37 | Slice 1d — SRC source-identity classifier | Honest status drift (half b) SHIPPED in slice 1c-1 (2026-07-15, R16 in docs/specs/onboarding.md — status compares live .bee/bin bytes vs the onboarding managed-hash ledger; docs/history/codex-harness-hardening-1c/). REMAINING (this row, slice 1d): the full SRC-01..06 tree classifier — source_checkout / project_projection / plugin_package / legacy_global / unknown — as a shared module in `templates/lib/` (+ mirror), imported by both onboarding and `bee status` (DIST-04 identity half): canonical marker wins for source (SRC-02), unknown fails closed before mutation (SRC-04), self-skip only on realpath identity (SRC-05), legacy global reported/migrated never an implicit source (SRC-06). (b) Honest status drift (E-02/DIST-04/PROJ-08): `bee status` must stop reading `drift:false` over a downgraded `.bee/bin/lib`. OPEN DESIGN QUESTION — no authoritative version reference ships into a host `.bee/bin/` today (release manifest is repo-only; manifest-hash.json tracks the command registry, not lib bytes; lib/state.mjs BEE_VERSION is itself rewritten by a downgrade). Needs its own exploring: what shipped artifact is authoritative (plugin.json version? a vendored lib-content hash written at onboard by a TRUSTED source only?). Deferred because the preflight (1b) already prevents the onboard downgrade path; this is defense-in-depth for other paths (source: codex-harness-hardening SPEC §6.2/§6.3; 2026-07-15) | done | codex-harness-hardening-1d 
| P31 | `bee.mjs <group> <verb>` is the only CLI an agent ever sees or ships | Shipped: the 9 legacy `bee_*.mjs` shims deleted from templates AND removed from hosts by a new onboarding `remove_helper` retirement pass (idempotent, exact-name-scoped, proven by self-onboard); AGENTS block, all skills prose, tests, installer, and living docs teach only the dispatcher; write-guard keeps legacy-shape recognition as an explicitly-expiring transition guard (P3 debt filed). Decision bbc6bcea · Spec: onboarding.md R12-R14, hook-runtime.md R14 · History: docs/history/shim-retire/ | done | shim-retire |
| P28 | Multiple terminals work one project without stomping each other, and a finished session hands work to a fresh one | Shipped v0.1.33 (13 cells, S1-S5): session-owned atomic claims with TTL+heartbeat proven by multi-process races on linux AND win32; per-feature lanes additive over state.json (zero-lane byte-parity held by the pinned suite); hook-enforced cross-session hold deny naming holder+expiry, phase-independent, fail-closed on a corrupt store; two-kind handoff (planned-next auto-resumes only at clear/startup via verb-guarded write + gate-based claim adoption; pause and kindless = wait, unchanged); claim-next pulls only from execution-approved lanes with in-pass stale-claim sweep and an honest "no approved work left" stop; doctrine + census anchors aligned. Headless outer-loop deferred (P29). Spec: docs/specs/workflow-state.md B11-B16, R17-R21 · History: docs/history/fresh-session-handoff/ | done | fresh-session-handoff |
| P27 | A stuck worker consults a configured advisor before returning [BLOCKED] | `models.<runtime>.advisor` slot (model or CLI, review-slot precedent) resolved by the rescue ladder's new consult rung: worker consults after its first serious failed verify attempt, max 2 evidence-bundled consults/cell, advice-only (never approves gates or overrides locked decisions); authority-type blocks stay instant-[BLOCKED]; degenerate consults skipped; consults recorded so scarcity stays measurable (source: owner request 2026-07-13; amends de967733 — rescue rung only, ONE cost pattern stays the default) (shipped 2026-07-13: resolveAdvisor + bee-executing consult loop + bee-swarming Advisor line, cells adv-1..3, history docs/history/advisor/) | done | advisor |
| P26 | Full independent review runs only when the user explicitly requests it | Ordinary cell/feature completion still requires verification evidence but spawns no reviewers; completed work can close as unreviewed and accumulate into a user-selected immutable review batch; status distinguishes verified/reviewed/stale, high-risk work warns without silent token spend, and Gate 4 exists only inside a requested review session (decision 565e68d0; spec: docs/history/review-on-demand/SPEC.md) (shipped 2026-07-12: docs/history/review-on-demand/ — 7 cells, acceptance map reports/uat-scenarios.md) | done | review-on-demand |
| P23 | Bee runs one unified fan-out pattern: best model orchestrates every phase, mechanical work goes down-tier | Advisor mode removed (reverses decisions 0013/0015; scope per CONTEXT); a delegation contract extends tier dispatch beyond swarming to all phases — main session keeps decide-altitude (gates, judgment, state writes, human chat), gather/render/mine steps dispatch to extraction/generation workers returning digests; delegation threshold keeps tiny tasks round-trip-free (source: owner decision 2026-07-11 after fable-orchestrator cost audit) (shipped 2026-07-12: Delegation contract + advisor removal, v0.1.23) | done | fanout-delegation |
| P18 | Bee learns from its own dogfood data and ships the improvement (evolving loop) | Slice A shipped: `feedback-digest.json` allowlist (6 structured fields, no free text), consumer-side revalidation of every foreign field (D2b), `dogfood_repos` merge, and a `bee-compounding` close-time refresh that warns and never blocks. Slice B shipped: `normalizeTitle`/`clusterEntries`/`rankClusters` + `bee_feedback.mjs rank`, the `bee-evolving` skill (bee-repo-only guard D3 → rank → Gate A → Iron Law hand-off D4 → suites green → Gate B → push D5) pressure-tested RED-first, hive routing wired, `docs/decisions/0022-evolving-loop.md`. Security boundary proven by a per-field payload sweep; real corpus 59→59 lossless; corroboration shipped defined-but-measured-inert (0 real cross-repo collisions, anphabe-gogl 59-entry digest, 2026-07-10). Spec: docs/specs/feedback-digest.md · History: docs/history/evolving-loop/ | done | 0.1.19 |
| P16 | Review work can pin its own model, separate from generation | An optional `review` slot in the `models` map (default: falls back to generation) resolved by bee-reviewing's specialist dispatch, fresh-eyes, and the plan-checker — so e.g. Opus xhigh reviews what Sonnet implements (source: owner's Fable/Opus/Sonnet/GPT role-split, 2026-07-10) (built in 0.1.18: review slot in models map, default opus, generation fallback, cli allowed; decision 0021) | done | 0.1.18 |
| P17 | A tier can carry a reasoning-effort knob | Tier values accept `{model, effort}` (and `{kind:'cli'}` keeps effort inside its command); resolveTier returns effort for runtimes that support per-agent effort — high for workers, xhigh/max reserved like ceiling (source: same role-split; Claude Code Agent effort param) (built in 0.1.18: {model, effort} slot values, EFFORT_LEVELS validated, applied where runtime supports; decision 0021) | done | 0.1.18 |
| P1 | A greenfield repo with no build gets an init lane on its first onboard | First onboard of a repo without a build offers the init lane instead of adopt-later (built in 0.1.17: onboarding notice + hive/planning init-cell convention) | done | 0.1.17 |
| P2 | Backlog rows can be ranked automatically instead of hand-ordered | A priority-scoring pass orders the rows once manual ordering no longer scales (built in 0.1.17: bee_backlog.mjs rank — status-grouped, stable, cell-preserving) | done | 0.1.17 |
| P3 | Backlog status renders as README badges | The README shows done / in-flight / proposed counts as badges (built in 0.1.17: bee_backlog.mjs badges — shields.io block between BEE markers) | done | 0.1.17 |
| P4 | Gates 2–3 are reviewed on a single human-readable implement plan | `bee-briefing` renders `docs/history/<feature>/implement-plan.md` from the truth artifacts; the gate message links it as the review object (design: docs/11-implement-plan-adoption.md; decision 0008) | done | bee-briefing |
| P5 | Capture-mode engages in-flight, not only when a human remembers | `scribingDebt` (behavior_change cells capped after last scribing run) surfaces in chain-nudge + preamble + bee_status so mid-swarming settlements reach `docs/specs/` without a manual invoke (finding: anphabe-gogl dogfood; decision 0011) | done | 0.1.8 |
| P6 | Model tiers are config-driven and runtime-keyed | `.bee/config.json` `models` (claude/codex → extraction/generation/ceiling) + `modelForTier` resolver; swarming resolves tier→model, ceiling kept scarce (design: Anthropic advisor/orchestrator patterns; decision 0012) | done | 0.1.9 |
| P7 | Keep the strong model scarce, measurably | `tier` field on cells + `tierMix`/`ceilingScarcityWarning`; ceiling-share surfaced in bee_status + preamble, planning assigns tier, swarming resolves cell.tier (decision 0012) | done | 0.1.10 |
| P9 | Exploring teaches before asking when the user is in unfamiliar territory | A blindspot-pass branch in `bee-exploring`: when the user signals unfamiliarity with the domain, teach the concepts needed to answer before Socratic locking — a decision locked from a guessed answer is a fake decision (source: Thariq map-vs-territory article, "blind spot pass") (built in 0.1.17: teach-before-ask branch in exploring; decision 0020) | done | 0.1.17 |
| P10 | Gate 4 walkthrough can quiz the approver | Walkthrough mode of `bee-briefing` optionally appends 3–5 questions about the change; mechanizes the gate litmus "a gate the user cannot restate is a dead gate" (source: Thariq article, "quizzes") (built in 0.1.17: optional quiz in walkthrough mode; decision 0020) | done | 0.1.17 |
| P11 | SEE gray areas can be locked by reacting to a throwaway HTML mock | Deliberate exception to exploring's no-code rule: a disposable mock (in `.spikes/` or alongside CONTEXT.md) for visual gray areas the user knows-when-they-see-it but cannot describe (source: Thariq article, "brainstorms and prototypes") (built in 0.1.17: .spikes mock exception in exploring; decision 0020) | done | 0.1.17 |
| P12 | The orchestrator measures worker results itself before accepting them | Swarming re-runs the cell's verify command (or lane-scaled spot-check) after `[DONE]` before counting a wave clean — miss re-dispatches; plus a frozen-judge rule: a worker diff touching test/verify config or lockfiles is flagged for review, never auto-accepted (source: LOOP survey — smart-orchestrate "evidence not assertion", delegator frozen-judge) (built in 0.1.17: goal-check in swarming + frozenJudgeHits + bee_cells judge; decision 0018) | done | 0.1.17 |
| P14 | A worker tier can resolve to an external executor (multi-provider swarm) | Extend the `models` map so a tier may name an external CLI dispatch (e.g. `codex exec`, GLM/Kimi CLIs) instead of an Agent model; swarming spawns it as a background process (prompt file + env var, status polled from job output) under the existing bee-executing contract — requires P12 first (design: LOOP survey — codex-orchestrator-graph dispatch, delegator adapters; managed-agents cookbook economics) (built in 0.1.17: resolveTier cli kind + dispatch protocol; first external run 2026-07-10: codex review dispatch incl. resume rescue; finish contract + accept-by-file added in external-result-contract; decisions 0019, 29b7f7bb) | done | 0.1.17 |
| P15 | Settlements are recorded without blocking the flow | Capture stub same-turn + deferred flush via `.bee/capture-queue.jsonl`, flush at wrap-up / PreCompact / next session; fresh-eyes and plan-checker run in background and block only at the gate; high-risk lane keeps full inline sync (decision 0017; built in 0.1.16, not yet dogfooded) | done | 0.1.16 |
| P19 | Bee updates its own skill set by script, not by hand-copy | Skill-sync mechanism in the onboarding flow: one command updates vendored helpers AND the installed bee-* skills, with safety semantics (downgrade guard, stale-skill handling). Feature: skill-sync History: docs/history/skill-sync/ | done | 0.1.19 |
| P22 | Every subagent dispatch is audit-logged with its resolved model/tier | bee-model-guard (PreToolUse, already sees every Agent/Task dispatch) appends one line per evaluated dispatch to `.bee/logs/dispatch.jsonl` — ts, tool, transport (model-param / marker / bare-denied), model, tier, subagent_type, truncated description — fail-open: a log failure never blocks or denies (source: owner request 2026-07-11, model-name visibility in dispatches) (built in dispatch-log: logDispatch in bee-model-guard, 5 new test rows RED-first; history docs/history/dispatch-log/) | done | dispatch-log |
| P20 | Socratic questions pass an explicit materiality test before being asked | bee-exploring's Socratic-locking step gains the three-part filter from grill-for-unknowns: *material* (answer changes scope/architecture/UX/acceptance), *grounded* (cites scout evidence), *answerable* (user can actually choose/approve/supply a reference) — two of three are already half-present; prose-only edit, no new skill (source: nicobailon/grill-for-unknowns; research: docs/history/research/grill-for-unknowns.md) (built in grill-deltas: materiality-test bullet in exploring step 4 + Red Flags line; history docs/history/grill-deltas/) | done | grill-deltas |
| P21 | CONTEXT.md pins fuzzy domain terms in a glossary as they crystallize | context-template.md gains a Terms section; exploring pins a fuzzy term the moment Socratic locking settles its meaning, scribing inherits pinned terms into specs — genuinely absent today (checked template, exploring, scribing); prose-only edit (source: nicobailon/grill-for-unknowns domain ledger; research: docs/history/research/grill-for-unknowns.md) (built in grill-deltas: ## Terms table in context-template, pin rule in exploring steps 4/5, Terms→Data Dictionary line in scribing; history docs/history/grill-deltas/) | done | grill-deltas |
| P65 | An agent loads only the knowledge its current task needs, within a token budget | `bee knowledge context --work <id> --budget <tokens>` walks the work item's `required_context` transitively, adds active decisions for its areas and every critical pattern, and returns a ranked manifest with size estimates and an inclusion reason per entry — never content. This is what makes a post-compaction session reload the *relevant* concepts instead of re-scanning docs/history. Ships inside P64 (okf-foundation D27), not deferred | done | okf-foundation (D27; area-decision tier is fixture-covered — the live bundle holds no bee.decision concept yet) |
| P66 | Every area spec becomes small typed concepts, and the legacy docs tree is retired | Migrate `workflow-state.md` (1464 lines) using the nine-concept map locked in okf-foundation D30 — overview/gates/cells/handoff/recovery/multi-session/review-sessions/dispatch/advisor-consult, with the 58 R-rules and 20 pointer bullets distributed to the concept each governs — then the remaining 10 areas out of `docs/specs/`; F2 planning MUST weigh the advisor flags in docs/history/okf-foundation/reports/advisor-digest-s1.md (D21 per-level committed indexes as conflict/flake amplifier; D35 exactly-one ownership vs genuinely shared rules) into `docs/knowledge/areas/<area>/`, migrate in-flight and historical work items to the `work/<id>/` shape, then delete the pointer stubs once every consumer has moved. Decides how much of `docs/history/` is promoted versus left as archive (deferred by okf-foundation D25 — one area only in F1) | done | okf-migration-f2 (11/11 areas, 476 anchors, 0 lost; stubs remain until F3) |
